# Issues — tile-lighting

_Defects found during execution land here. Known inputs: tile prims may already be minting
UNINTENDED records the moment textured tiles exist (the P0 audit decides); the hot-sync P4
correction model assumes the cold map under a mover is STATIC — a tile that changes beneath
a standing mover re-bakes cold under hot deposits (watch the ordering); art-128's texture
migration remains in flight._

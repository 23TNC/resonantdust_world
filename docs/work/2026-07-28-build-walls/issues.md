# Issues — build-walls

_Defects found during execution land here. Known inputs: the tile-override machinery in
`WorldBridge` (`coldPrims` baseline/override interplay) predates neighbor-aware re-expansion —
watch for stale variants when an override wins a cell; and the texture-layout migration
(art-128) is mid-flight — wall masters may move under us the way the wolf's did._

# Blockers — render performance

_What stops forward motion, and who can clear it. Empty is the healthy state._

None. The geo tier already exists and draws unconditionally, so P1 removes a barrier rather than
building a replacement.

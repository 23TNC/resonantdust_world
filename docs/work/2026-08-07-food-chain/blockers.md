# Blockers — food chain

## B1 — the user's eyes close the stream

Everything ticked and live-drilled (see completed.md): the trigger law, the leveled
corpus mint, death → meat → removal, forage/eats, the warren + the eating wolf, the
outlined placeholders. What remains is the USER seeing the loop run — recommendation:
watch the warren at (113,68) with `?ambient=0.8` for a few minutes (bunnies roam,
drink, eat), then kill a bunny from the console
(`window.__client.queue(new Uint32Array([10, <entity>, 0x30]))`) and watch the meat
drop and a hungry wolf take it. Close on sight.

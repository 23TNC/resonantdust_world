# Blockers — bug sweep

## B1 — the user's eyes close the stream

Everything ticked and drilled (completed.md): the z-order tiers (click chat/build
to feel the tie-flip; check settings floats over everything and On Top is gone
from the panel settings popup), zero geo flashes on direction changes, the wall
pen refusing + routing around, the union outline (select a human — the neck is
clean), and the vanish evidence file (I11 — the P2 fix is the strong suspect;
the re-armable probe snippet is in the issue if you ever see one again). Close
on sight.

# Blockers — emotions

## B1 — the user's eyes close the stream (open, 2026-08-07)

Everything else in the plan is ticked, drilled, and committed: the 16 emotions live in
the corpus, mood is gone, the argmax runs in npc + client + tests, the pies/tooltip/wash
are captured, the bounce ran green. What remains is the convention's final step — the
user LOOKS at the live panel and closes the stream (the index row then flips to
delivered). To look: select any pawn; `__cards(6)` pins synthetics; the oracle set is
still granted on 0x30800002 (happy-gold wash) and the wolf carries the stacked set
(uncomfortable-olive wash). Recommendation: close on sight; no open defects.

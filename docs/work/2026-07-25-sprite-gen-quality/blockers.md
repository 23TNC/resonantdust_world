# Blockers — things that need the user

_None open._

One item is **user-facing by design, not blocked**: P4's curation step needs the user's keep/reject
picks on the generated contact sheets. That is the flywheel working as intended — the human is the
final filter — not a blocker to record here.

# Todo — coord-purge

**All items (A–G) are done — see [`completed.md`](completed.md).** Nothing outstanding.

The one open follow-up is not a todo item but a hand-off: **F's browser pixel-confirm** — re-check
terrain on `:5173`/`:5174` after a dev-loop refresh that serves the rebuilt wasm (a vite restart).
Behaviour-preserving by construction (`macro_world_origin ≡ zone_world_origin ∘ ≪8`), so no pixel
should move from the already-confirmed seam-fixed state.

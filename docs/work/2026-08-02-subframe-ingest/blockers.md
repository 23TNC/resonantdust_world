# Blockers — subframe at ingest

_What stops forward motion, and who can clear it. Empty is the healthy state._

None. The seam exists (`packCoPack` already takes one `AtlasDraw` per co-pack), the DSL has a working
precedent for a layout number applied at ingest (`sprite_scale`/`sprite_anchor`), and the def's GREEN
word is already free for the anchor lane.

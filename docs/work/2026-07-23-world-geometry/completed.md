# Completed — world geometry

_Delivered + verified. Newest first. Model in [`README.md`](README.md)._

---

_(none yet — planned. Groundwork this stream formalizes:)_

- **The model itself was derived + simulation-confirmed with the user (2026-07-23)** — billboards
  parallel to the view plane, ground at 65° to it, `z = sin65·(px.y − base)` per-prim, `s` from heights
  only, **x separable**, and the **left/right y-equality** that reduces a billboard's shadow to one y
  calculation + two projected top x's + the billboard's own bottom corners. P0 moves it into a durable
  home; it is ratified, not speculative.

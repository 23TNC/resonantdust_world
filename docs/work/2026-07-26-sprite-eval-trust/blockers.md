# Blockers — things that need the user

_None open._

P4's retrain is a multi-hour GPU job on the user's home-automation box; that is a **cost to flag
before launching**, not a blocker to record here.

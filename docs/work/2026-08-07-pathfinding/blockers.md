# Blockers — pathfinding

## B1 — the user's eyes close the stream (open, 2026-08-07)

Everything else is ticked, drilled, and committed: water and trees close their cells,
the shared A* runs in worker + client + npc, fells reopen, drinks stop at the
shoreline, the bounce ran green. What remains is the convention's final step — the
user LOOKS and closes (the index row then flips to delivered). To look:
`?user=Claude&focus=104,74&zoom=0.5&ambient=0.8` frames the lake; order any pawn
across it (the drill pawn 0x30800004 rests at (95,75), the felled cell with logs is
(114,72), wolves live on the east shore with home 108,70). A click ON water quietly
no-ops (worker log line); menu graying for water is the recorded successor (I5).
Recommendation: close on sight; no open defects.

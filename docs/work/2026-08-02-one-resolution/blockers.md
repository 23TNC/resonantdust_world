# Blockers — one resolution

## B1 — P6's remaining items are gated on things outside this session {#b1}

**What blocks**: (1) the BLACK-FLORA root cause (layers quadrant empty on both twins,
disk bytes healthy — [I4](issues.md#i4)) lives in the independent-per-map pack path the
ARMED `render-performance` session is actively reworking; fixing it here would collide
mid-flight with their I2 work in the same functions. (2) The pan-at-extremes item needs
a FOREGROUND tab — my driven-frame harness renders correctness but pan artifacts are a
live-eye class. (3) The "z-order" item most likely REDUCES to the black-flora defect
and should be re-judged only after that fix lands.

**Why it needs you**: sequencing across two armed sessions is yours to call, and the
foreground-tab drill wants your screen (or a moment where my tab may hold focus).

**Options**: (a) let render-performance land its per-map re-pack fix, then I re-verify
items 3–5 here (recommended — one owner per seam); (b) authorize me to fix the flora
re-pack in their seam now (fastest pixel fix, mid-air-collision risk); (c) roll the
one-resolution stream back (recommended AGAINST: the pre-stream A/B froze the renderer
under load — the old invalidateAll stampede — and the preview tier is already restored
at HEAD).

**Recommendation**: (a), plus one foreground pan drill with you watching — five
minutes, both zoom extremes, and P6 closes one way or the other on your eyes.

**RESOLVED 2026-08-02**: the user chose forward-fix over rollback and asked for a
consolidated stream — [`2026-08-02-lod-aftermath`](../2026-08-02-lod-aftermath/README.md)
absorbs P6's remainder (flora/lighting/z-order/pan) with the render-performance
coordination built into its P0. This stream is DONE; the aftermath stream carries the
bugs.

# Blockers — chord movement

## B1 — the user's eyes close the stream (open, 2026-08-07)

Everything else is ticked, drilled, and committed: pawns walk minimal-chord shortest
paths at Euclidean tiles/tic (a 6.803-tile trip landed in exactly 164 tics), positions
carry subtile sixteenths, mid-chord interrupts resolve in place on the server AND turn
in place on the client (the seed fans no position; promoted hops every ≤32 tics are the
re-anchor), and the wolves estimate in chords with zero deadline churn. What remains is
the convention's final step — the user LOOKS and closes. To look:
`?user=Claude&focus=104,74&zoom=0.5&ambient=0.8` frames the lake; order the drill pawn
(0x30800004) somewhere far and watch the DIAGONAL glide (no stair-steps), then order it
elsewhere mid-walk — it turns where it stands. Honest note: Chebyshev's free diagonals
are gone — diagonal trips take true Euclidean time now, so pawns read slightly slower
on 45° runs (physically honest; content tuning owns pace).
Recommendation: close on sight; no open defects.

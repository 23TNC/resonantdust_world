# Issues — ui-select

_Defects found during execution land here. Known input: the cursor light is the FIRST live
hot light — if the hot-light matrix cells (hot light × cold prim, hot light × hot prim)
misbehave, the defect belongs to pawn-render's structural-only verification, recorded here._

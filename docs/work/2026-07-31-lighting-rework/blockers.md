# Blockers — lighting + shader rework

_No open blockers._

Two rows were filed here on 2026-07-31 and moved the same day, on the user's correction: both carried
"does not block" in their own text, which is the tell that they were never blockers.

- The three capabilities the strip removed → [`forks.md` F11](forks.md#f11). They are **features not
  implemented in this pass**, which is a scope decision, not a question that stops work.
- The 8-lights-per-tile cap → [`issues.md` I7](issues.md#i7). An inherited artefact to revisit with
  measured cost, which is a finding.

The bar this file exists for: something that **stops execution** until the user supplies what only
they can. A question I can answer, or a decision I can make and record, belongs in `forks.md`; a thing
I have learned belongs in `issues.md`.

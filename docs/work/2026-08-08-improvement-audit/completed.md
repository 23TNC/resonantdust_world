# Completed — improvement audit

## 2026-08-08 — the sweep, the triage, the ranking

**P0** — findings.md scaffolded (dated header, rubric, six lanes, deliberate
postures); docs-check green with the file linked from the plan.

**P1** — all six lanes swept. Coverage: every index-open stream (46)
contributed or is inventoried — the 2026-08-06..08 engine streams from this
session's own execution context, the older engine + art streams by two
parallel reader agents (their per-stream open-issue extracts integrated as
the Lane 1 addendum; art condensed per I4's bound). Docs-check's four
warnings each became a candidate (D1/D2/D3) or a stated non-issue; the perf
streams' four questions are entries (P1–P4); the code-sweep grep commands
are recorded beside their counts (16 TODOs — 15 in generated bindings; the
worker's corpus-unwrap panic C1 the one real hit); the architecture lane
cites where each future was named; every DX entry carries the incident it
cost time in (the docs-stub race X5 struck TWICE today — the second victim's
issues.md names this session).

**P2** — every candidate carries a status; probes were read-only (greps, log
reads, docs-check output — nothing mutated the world). The ranked table
stands at the top of [findings.md](findings.md): 15 ranked entries + the
below-the-line set. The headline structure: ONE high-stakes UNKNOWN
(lighting-rework's self-declared REOPENED shadow defects — verify against
the later streams before believing either way), three A-class live
candidates (subframe cell-0 fight, the stuck-geo-tier repack hole, the
unpinned zoom drift), a B-class server cluster that SEQUENCES (satisfy
transaction → orchestrator single-owner → second worker), and a one-session
DX sweep that pays back daily.

Verified: the document stands alone (every entry carries its evidence
pointer); docs-check green. The user's eyes close the stream and choose
what becomes work.

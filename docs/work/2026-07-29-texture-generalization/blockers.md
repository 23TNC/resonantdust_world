# Blockers — texture-generalization

_Rows: what blocks, why it needs the user, options, recommendation._

_None open. (A former B1 — "art masters broken, world can't render" — was a misdiagnosis
and is RETRACTED 2026-07-29: the post-outage scramble was the manifest corruption
(`biome-thing` nested inside `biome-tile/default/`, since fixed), and near-black `albedo`
masters are BY DESIGN — albedo carries outlines, color arrives via material/tint layers at
composite. The world renders correctly; visual acceptances are unblocked.)_

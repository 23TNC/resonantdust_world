# Blockers — inventory

## B1 — the user's eyes close the stream

Everything ticked and live-drilled (completed.md): the free-slots need, the
sub-table + fan, pick_up/drop through the REAL pie menus, the reworked details
panel (name + tile + the button row), the slot-grid panel with reload-persist,
and the full bounce. What remains is the USER seeing it — recommendation: select
the human_female near (113,70) (she holds a log), open [Inventory], drop the log
from its slot, pick something else up off the ground, and confirm the names in
the details panel read right (`shrub`, `reed`, `grass`…). Close on sight.

# Work — index

_The flowing execution state. Each `work/<w>/` turns component `plan`s into executable items and
spans components by nature. Convention: [`../CONVENTIONS.md`](../CONVENTIONS.md). Last updated:
2026-07-27._

**Status** = the stream's lifecycle, not the session's focus: `open` (in progress) · `done`
(delivered; may have a small hand-off) · `closed` (abandoned/refuted) · `blocked` (needs input).
The continuation hook's notion of the **active** stream is *session-scoped* (which open stream this
session is driving — inferred, not a field here); see [`docs-authority`](docs-authority/README.md) P5.

| Stream | Status | What it is |
|---|---|---|
| [2026-07-27-plane-intersection](2026-07-27-plane-intersection/README.md) | open | **Replace the projected-quad containment test with a direct light→pixel plane intersection** (user, 2026-07-27): _"each and every point is casting the full quad and then checking if they're in that quad. That… is ridiculous."_ `casterCover` answers "is P shadowed" TWICE — `shadowCover` builds the caster's whole projected quad (tilt sin/cos, `k`, two `projectTop` calls with a `sqrt` each, four corners, four `cross2`) and point-tests it, then the tap loop re-derives the same fact by inverting the light→P ray to the card's `(s,t)`. That inversion IS the plane intersection, already written and already tilt-correct, so the quad is a redundant pre-filter ([I1](2026-07-27-plane-intersection/issues.md)). It is paid on MISSES, which dominate — the corridor visits ≤8 casters/tile over ~20 tiles and only a handful shadow any texel — which is what makes the walk 77 % ALU. P0–P2 is a pure bit-identical refactor (prove the predicates agree, delete `shadowCover`/`projectTop`, restore the `reachU` bound and `SHADOW_BASE_PUSH` explicitly, measure). P3–P5 then build the user's ± radius scheme on the simplified function: two rays at light ± emitter radius classify umbra/penumbra/lit, a straddle rule fills the wedge behind the caster, and the caster BUCKETING box gets padded by penumbra reach — the second cause of the tile-boundary hard clip, which the gate change alone does not fix. |
| [2026-07-27-sim-self-heal](2026-07-27-sim-self-heal/README.md) | open | **Make the sim processes survive shard outages** (user, 2026-07-27). `master`/`worker`/`orchestrator` `.expect()` every upstream connection + the 5s subscription wait at standup and never rebuild a dead connection mid-run — one briefly-down shard kills the process, and a mid-run disconnect leaves a zombie loop. The gateway already solved this (`directory.rs`: `alive` flag cleared by the SDK + rebuild-on-next-use + subscribe-and-wait). Plan: lift that pattern into a shared **`server/uplink`** crate (closure-built per module type, sub-`on_error`-aware, capped backoff — [F1–F3](2026-07-27-sim-self-heal/forks.md)), convert all three binaries so a pass skips only work whose uplink is dead (correctness rides existing idempotence: replay-safe writes, stateless grouping, durable tic), and fix the adjacent permanent-fault hazard: `server/edge`'s asset paths `.unwrap()` `RwLock`s, so one panic while holding a lock poisons content/texture serving until restart. Acceptance is live chaos: restart SpacetimeDB mid-run → all three reconnect without process restarts and wolves resume moving; start them with SpacetimeDB down → they come alive when it does. |
| [2026-07-26-moving-lights](2026-07-26-moving-lights/README.md) | open | **Implement a real method to MOVE a prim, then work out what lighting can actually survive it** (user, 2026-07-26). Two facts drive it. **(1) There was no move — FIXED 2026-07-26.** A carrier prim's position record had exactly one writer: **allocation**. `carriedLightFor` wrote `PRIM_BASE + prim`'s position only inside its `prim === undefined` branch, and `writeCarriedLight` then re-emitted the record passing the *existing* position word, explicitly preserving the stale value. Survivable for a light WITH a billboard (`billboardDataFor` rewrites the same record every frame) and fatal for one without: all three content torches measure `def = −1` — they carry no sprite — so `buildCasters` `continue`s before the billboard path and nothing else ever touched the record. Measured before: `primMoved: true, recordMoved: false`. Fixed by refreshing the **position lane only** (G + id lanes carried through, so the carried-piece slots survive) and threading `from` out to `markLightDirty`, which closes [I1](2026-07-26-moving-lights/issues.md#i1) in the same change. Verified at the OUTPUT: `debugReadShadow(0)` hashes **1561431880 → 3834622774 → 4097208919** on populations ~101 300, where it had been a single constant with the orbit on AND off. **This corrects [primitive-graph I40](2026-07-25-primitive-graph/issues.md)**, which blamed `stepOrbit` for writing a discarded per-frame read model — `standingPrims()` pushes **references**, so those writes always landed; the observation was real, the mechanism was invented, and it went into three documents before I checked the four-line method that disproves it. The I37/I39/I40 lesson still holds and is what found the real cause: *a proxy near the START of a pipeline is not evidence about its END*, so **every acceptance here reads the output**. **(2) The sharing model is right; the invalidation granularity is wrong.** Lights bake into a shared exact-integer additive accumulator, so a static light bakes once and is free forever — **123 static lights hold 120 fps at 0.72 ms**. But a light's dirty unit is its **entire reach disc, full quality, in one frame**: the same 123 lights *moving* cost **72.44 ms / 15.2 fps**, 100× for nothing but motion. **The zoom-in cliff, and the cost model — settled on the THIRD attempt** ([I4](2026-07-26-moving-lights/issues.md#i4); the two superseded models are kept in [I2](2026-07-26-moving-lights/issues.md#i2)). Every buffer is **constant** across zoom (shadow RT 512 × 256, lightmap 4096 × 2048, measured), so holding the dirty set at 100 % isolates zoom: **zoom 1 / reach 8 → 19.66 ms**, **zoom 0.25 / reach 8 → 8.25 ms with 16× MORE world tiles (the cheapest run of the session)**, **zoom 0.25 / reach 32 → 63.25 ms on the same tiles**. **The map's tile count does not enter the cost.** The model that fits all three: `cost ∝ (texels a light covers) × (tiles the corridor walks per texel)`, a light covering `π·R²ₜᵢₗₑₛ × 16384/4^lod` texels — row 3 predicts 4× row 1 (78 vs 63.25 measured). Both earlier models died: *cost ∝ dirty fraction* (refuted — 8 192 dirty tiles was the cheapest run, because **a dirty texel with no light in range is nearly free**; "dirty" means recompute, not expensive) and *reach dominates* (true but a symptom — reach enters through both terms). Each had been fitted to a sweep that varied one input, then stated as a law. **The user's "it scales with px, not tiles" is right, and the waste is quantified:** the lightmap integrates **4096 × 2048 = 8.39 M texels for a 2560 × 1172 = 3.00 M px canvas — 2.79×**, from the fixed 3584 × 1536 reference (1.83×) times 32 × 16 slots vs 28 × 12 visible (1.52×). The reference should govern **what world is visible**, not **how many texels we integrate**; they are welded today. Zoom-in genuinely costing more (a light covers 4× more of a fixed-size map per lod step) is inherent; the 2.79× is not — **P4**. Corollary: **reach 16 against a 28 × 12-tile screen is not a torch, it is ambient light** ([F3](2026-07-26-moving-lights/forks.md#f3)). Levers, chosen on P0's numbers not now ([F2](2026-07-26-moving-lights/forks.md#f2)): hot lights bake a **coarser lod** (16× fewer texels, reuses [textile-slot P4](2026-07-26-textile-slot/todo.md), must respect the coarsest-since-cast rule or light leaks) · bound reach against the visible world · amortise the disc over N frames · and **measure the empty-corridor fast path FIRST**, since if most of a disc has no caster and still pays full price the architecture was never the problem. Rejected on arithmetic: per-px screen-space hot lights (~2M px × N walks ≫ 1 000 tiles × 256 texels) and delta re-bake (correct, but evaluates the light twice). Also fixes [I1](2026-07-26-moving-lights/issues.md#i1): the one call site that fires on a light change omits `markLightDirty`'s `from`, so a mover dirties only its NEW box and trails stale shadow — invisible until now because **nothing has ever moved**. |
| [2026-07-26-torch-thing](2026-07-26-torch-thing/README.md) | open | **Implement torches PROPERLY — real THINGS seeded in spacetime at init, full stack end to end** (user, 2026-07-26). The current torches are a stopgap: [primitive-graph P11](2026-07-25-primitive-graph/todo.md) got light on screen via a **biome scatter** (`0.006 lt if torch`), which makes a torch *terrain* — probabilistic (so no test can assert "the torch at (12,9) lights these tiles"), absent from already-generated zones (spawn has **0 torches across 3,429 prims** while a fresh region has 74 — [B-4](2026-07-25-primitive-graph/blockers.md)), and with no identity anything could ever interact with. Target path: a kind-agnostic **`place_things`** reducer on the `thing` module → cold-thing OVERLAY → edge composite → WS → client `onColdThings` → `Primitive.light` → carried light leaf → presence → lightmap. Everything right of `onColdThings` already works; this replaces only the leftmost box. **Two findings ruled out the obvious approach.** A torch CANNOT be a mobile entity: `PLACE` carries position only (which is why the npc's wolves are debug circles), and since light is **per-kind**, a torch on the mover path would emit nothing and look like broken lighting ([I1](2026-07-26-torch-thing/issues.md#i1)). And the `thing` module CANNOT resolve `"torch"` — it links `spacetimedb` + `codec` with no DSL, so a module-side `init` could only place a **hardcoded** kind id, a second authority beside the DSL that desyncs the moment content is reordered ([I2](2026-07-26-torch-thing/issues.md#i2)). So the reducer is parameterised by `kind_reference` and the **edge** resolves it at the point of use, exactly as `current_worldgen()` already requires. [F1](2026-07-26-torch-thing/forks.md#f1) takes the OVERLAY over `seed` (which replaces a whole baseline row and would erase the zone's trees); [F3](2026-07-26-torch-thing/forks.md#f3) retires the scatter but keeps the KIND — sequenced AFTER verification so the world is never dark mid-stream. Dissolves B-4 and turns "count the lights and squint" into a named-cell assertion. Open: [B-1](2026-07-26-torch-thing/blockers.md) how many/where, [B-2](2026-07-26-torch-thing/blockers.md) whether "init" must mean a module `init` reducer. |
| [2026-07-26-textile-slot](2026-07-26-textile-slot/README.md) | open | **P0–P3, P5, P6 DELIVERED 2026-07-26.** Every textile map is now sized in TILES on a fixed **32×16 slot** grid (28×12 visible + 2 overscan/side, both axes pow2 so the toroidal wrap is a bitmask — [F5](2026-07-26-textile-slot/forks.md#f5)); a slot holds `2^k × 2^k` tiles at lod k, so the **texture is constant while the world it covers grows 4× per step**. `SQUARE` 64 → **128**, max art size 128px, `ZOOM_MIN` 0.25 ([B-1](2026-07-26-textile-slot/blockers.md)). Measured: **394 MiB resident, CONSTANT** across a full 1→0.25→1 sweep, against 264 MiB for the lightmap+shadow *alone* at zoom 0.25 before and still growing. **Zoom no longer flashes** — a lod change REPROJECTS instead of clearing ([P3](2026-07-26-textile-slot/todo.md)), so zoom-IN bakes **0 fresh** tiles and zoom-OUT bakes only the newly exposed ring; empty tiles drain before merely-stale ones ([P5](2026-07-26-textile-slot/todo.md)). Solved three unrelated problems at once: memory stopped tracking zoom, **gameplay stopped tracking monitor** (a fixed 3584×1536 reference + cover fit `s = max(W/3584, H/1536)` means every player sees the same world, a bigger display magnifying rather than revealing more), and the zone subscription now sizes itself from the tile window rather than a screen estimate that `SQUARE` 64→128 had silently halved ([I8](2026-07-26-textile-slot/issues.md#i8)). Two findings worth keeping: **lod does not affect sharpness** — the display ratio is `1/σ` and the `2^k` *cancels* ([I1](2026-07-26-textile-slot/issues.md#i1)) — and per-light lod state must be the **coarsest** since last cast, not the last, or a down-then-up round trip leaks light permanently ([I2](2026-07-26-textile-slot/issues.md#i2)). **NEAREST is the universal rescale rule** ([F3](2026-07-26-textile-slot/forks.md#f3)), independently mandatory for the shadow bitfield, `zdepth`'s encoded row, and the additive lightmap's exactness. Only **P4** remains (per-light `coarsest_lod`), blocked on [primitive-graph P10](2026-07-25-primitive-graph/todo.md) — whose **memory gate this stream cleared**: `RGBA32F` costs 128 MiB fixed here vs 465 MB world-sized. |
| [2026-07-26-sprite-eval-trust](2026-07-26-sprite-eval-trust/README.md) | open | **Make the measurement agree with the eye, then fix what run-4 broke** (user, 2026-07-26). Run-4 (fresh, 1024, dim48, 15 epochs on the P2 dataset) spent **4h39m and did not ship** — and the eval caught **neither** of its two failures: south rendered head-only portraits inside a drawn white rectangle yet **passed 10/18**, and east's pose drifted from lying to **sitting** while the metric called it a **tie** (34.8% vs 34.1%). Both found by *looking*. Diagnosis — the ruler has two structural blind spots: **`d_aspect` is unsigned**, so "too long" (a proportion wobble) and "too compact" (a broken pose convention) score identically though the sign was the whole signal ([I1](2026-07-26-sprite-eval-trust/issues.md#i1)); and **everything is bbox statistics**, which cannot see a bust *inside* the box ([I2](2026-07-26-sprite-eval-trust/issues.md#i2)). Fix = **`iou_ref`**, overlap against the real corpus sprite — explicitly NOT the already-refuted `iou_control` (which compared against the *control* and failed because a wrong control faithfully obeyed scores high); reference-IoU has ground truth, so it should catch both a sitting-vs-lying drift and a framed bust ([F2](2026-07-26-sprite-eval-trust/forks.md#f2), which also rejects more box statistics and defers a CLIP scorer as a black box where trust is the point). Plus **signed** aspect error, and **auto-emitted contact sheets** so the visual check that found the truth four times is part of the harness rather than a favour. Then **P2** jitters the pinned `fill` that taught the model to draw a margin ([I3](2026-07-26-sprite-eval-trust/issues.md#i3)) while keeping P2's measured gains, **P3** isolates the pose drift (4 candidate causes, run-4 changed all at once — [I4](2026-07-26-sprite-eval-trust/issues.md#i4)), and **P4** retrains and A/Bs against a **frozen** gate. Eval-before-retrain is re-affirmed ([F1](2026-07-26-sprite-eval-trust/forks.md#f1)) — the predecessor argued it and was proved right the hard way. Body-plan agnostic: the biped/legless/humanoid LoRAs reuse this gate and sheet harness. Predecessor's P4 flywheel + P5 coherence stay in [sprite-gen-quality](2026-07-25-sprite-gen-quality/README.md). |
| [2026-07-25-sprite-gen-quality](2026-07-25-sprite-gen-quality/README.md) | open | **Raise the quality ceiling on template-free generation** (user, 2026-07-25) — successor to [template-free-generation](2026-07-25-template-free-generation/README.md) (DONE 22/22). Triggered by the **anteater**: east is genuinely good (tapered snout, hunched back, bushy tail) but south/north collapse into a cape-like blob. **P0 fixes the RULER first** ([F1](2026-07-25-sprite-gen-quality/forks.md#f1)) because the gate has *both* error types — the blobby anteater **passed** at `d_aspect` 13.2%/4.1%, while a good horned oryx was **quarantined** at `solidity=0.349` vs a 0.35 threshold (failed by 0.001, since horns enclose empty bbox area). Retraining first would score the A/B with a ruler known to be wrong in both directions. Fix = **`iou_control`**, IoU of the output silhouette against the control it was given ([F2](2026-07-25-sprite-gen-quality/forks.md#f2); rejected more bbox statistics as the same failed class, and a learned scorer as an unexplainable black box). Then **P1** replaces the hand-written `FAMILY_REP` table — the anteater got Elephant because someone typed `pachyderm->Elephant`, while 396 bank silhouettes went unconsulted — with a shape-similarity search (`--control auto`). Then **P2**, the biggest single lever ([I1](2026-07-25-sprite-gen-quality/issues.md#i1)): `prep_train.py` upscales with **LANCZOS**, blurring edges on nearly every training image (most sources ≤256px), though this art is *vector graphics rendered as bitmaps* so an edge-preserving upscale **reconstructs** rather than hallucinates; plus subject **scale is unnormalised** (bear fills the frame, cat is tiny) so the LoRA learned scale is arbitrary. **P3** retrains + multi-seed A/B, **P4** runs the first full **flywheel** cycle (generate → screen → hand-pick → retrain), **P5** tightens cross-direction coherence (0.169 generated vs **0.028** real same-animal). Every comparison is ≥3 seeds — the predecessor's single-seed baseline reported a failure *rate* as a species verdict. Body-plan agnostic throughout: the biped/legless/arthropod/humanoid LoRAs reuse `auto` + the same gate. |
| [2026-07-25-template-free-generation](2026-07-25-template-free-generation/README.md) | done | **DELIVERED 2026-07-25 — 22/22, validated on 5 species absent from training (15/15 valid).** `--control template|corpus:<Species>|family:<f>|none`; the 396-image corpus silhouette bank replaces authored templates. **Generate species we have never drawn** (user, 2026-07-25). `bin/art generate` is *reproductive, not generative*: `load_template()` `SystemExit`s without hand-authored `textures/<kind>/template.<dir>.<part>.png`, so a new animal needs the art the generator was meant to save you. Now solvable because the `rd_quadruped` LoRA took over both of the template's old jobs — it carries the **style** (which moved the measured defaults to `dn1.0/cn0.20/cn_end0.30`, ~3.4x the seed variety), and at `dn=1.0` the template's **i2i latent is fully destroyed** ([I1](2026-07-25-template-free-generation/issues.md#i1)), leaving it a silhouette source only. The LoRA holds body plan alone (`cn=0.00` still drew a correct wolf). Measured limit: a template generalizes **within a body-plan family, not across** ([I2](2026-07-25-template-free-generation/issues.md#i2)) — leopard-on-wolf succeeded (genuinely feline features), brown-bear-on-wolf failed (face-in-torso, blobby east). Stance: control becomes a **mode** (`--control template|corpus|family|none`, explicit art still default+winning), the **corpus IS the template library** (459 on-model sprites x 132 species already in `.staging/quad-lora-train` — zero authoring, [F1](2026-07-25-template-free-generation/forks.md#f1) rejected authoring a family set as a strict subset), and **quantity + auto-screening replaces precision** (`lora_eval` scores every candidate; generate N, keep survivors) — which is also the input to the self-training flywheel. Body-plan agnostic on purpose: the biped/humanoid LoRAs reuse the same control path. |
| [2026-07-25-continuation-hooks](2026-07-25-continuation-hooks/README.md) | open | **Harden the continuation system** — the machinery that stops a turn ending while unblocked, executable work remains ([`docs-authority`](docs-authority/README.md) P5/P6 built it; this stream makes it robust). P6 fixed a hook that had been **silently dead for six days**; auditing that fix across all 34 streams found two more defects, bringing the tally to **eight classification bugs** — one root cause: *the detector infers machine state from prose written for humans, in four item dialects*, and it fails **open and silent**. Two real changes: **(1) stop inferring** — a `- [ ]`/`- [x]` checkbox becomes the only thing that counts as an item (already the majority dialect, 20/34), retiring the whole heuristic layer; **(2) make the plan executable** — corpus items are paragraphs, not actions (median 142 chars, 67 over 150), which is a *cause* of premature stopping, since a vague next step is itself a reason to check in. Plus visibility (decision log, `--doctor`, warnings) so the next silent death can't happen, a resume **brief** replacing the bare nudge, an execution-intent gate so conversational turns aren't nudged, and a `/plan-phase` decomposition skill. |
| [2026-07-25-primitive-graph](2026-07-25-primitive-graph/README.md) | open | **P0–P3b LANDED 2026-07-25** (see [`completed.md`](2026-07-25-primitive-graph/completed.md)): layouts authoritative in VARIABLES · command buffer **v3** (fixed 8-px commands, ids in the header, count scan gone) · all four records on v3 · presence at 8 slots → **16 lights + 8 billboards/tile** with `shadow-cold` repacked to **u7 coverage \| u1 on-billboard** · `prim_data` node + `billboard_data` leaf · **lights are CARRIED, never placed** · the **resolve walk** with downward-forcing inheritance (`MAX_PRIM_DEPTH = 8`, the single resolve authority). No open blockers. Remaining: real nesting + rotation reconciliation, retiring the `this.lights` array, P4 dirty generalisation, P5 authoring. **PREEMPTS the lighting/shadow streams — it rewrites the records they read.** (user, 2026-07-25) A **prim** becomes a generic **composition node**: positioned, carrying **up to 4 pieces of data** (`set_a..d` + `id_a..d`, set 0 = sentinel) which may be a **billboard**, a **light**, or **another prim**, each placed *relative to its carrier*; `hot_cold` inherits downward (topmost hot wins) and `cast_shadows` too (topmost no-cast wins). Generalises every object: torch = prim{billboard, light}; pawn = prim{head, body, hand-prim, hand-prim}; hand = prim{billboard, tool-prim}. New `billboard_data` set (def + offsets) splits from `prim_data` (position + carried ids); `light_data` carries colour/reach/radius **inline** (no light-def); `definition_data` gains `u4 type`. **Self-addressing retired** → ids ride the command as **fixed 8-px commands** (`opcode | set | 7× u16 ids` + 7 payloads = 56 writes/row; the leading opcode is a real extension point and the scatter drops its count scan), which buys back a `u16` in every tile-keyed px → **16 lights + 8 billboards per tile** (was 14/7). Presence carries **leaves** (`light_presence` / `billboard_presence`) — bucketing carriers would make per-tile billboards unbounded. **Rotation is a CPU reconciliation signal, not a render input**: the shader always uses the *definition's* rotation, and a mismatch prompts a definition swap (`parent_rotation` opts into carrier inheritance). All lanes verified to sum to 32; the RED position lane is already what `encodePosition` emits. All blockers resolved (bias-8 offsets, `parent_id` + `child` bit, rotation semantics, 8-px commands, `emitter_radius` restored, `resolved_zone` added). Subsumes [light-prims](2026-07-24-light-prims/README.md). |
| [2026-07-23-world-geometry](2026-07-23-world-geometry/README.md) | done | Establish **ONE canonical geometric model** and conform the code to it. Ratified with the user + confirmed by simulation: billboards drawn **parallel to the view plane**, ground at **65°** to it, **fictional height `z = sin65·(px.y − base)`** measured per-prim against that prim's own base, projection parameter `s = z_light/(z_light − z_point)` (pure heights ⟹ pure y), **x separable** (`G.x = light.x + s·(prim.x − light.x)`, never influences y), and **left/right y-equality** ⟹ a billboard's shadow is **one y calculation + two projected top x's + its own bottom corners**. The shadow code today runs a *different* leaning-card fiction (`2·tan65` vs `sin65`, ~4.7× apart) with `SHADOW_LIFT` papering over the seating — so "what is our geometry" has no single answer in the codebase. Blocks shadows-on-prims. |
| [2026-07-24-shadow-edge-refine](2026-07-24-shadow-edge-refine/README.md) | open | **Sharpen the coarse shadow's edge against the fine caster silhouette, at bake time** (user). The shadow map stays coarse (16/tile) — bumping the *gather* to full res is ~16× the **corridor walk** (the walk, not the silhouette raster, is the cost). But the blocky edge IS the caster silhouette sampled coarse; the fine lightmap bake already visits every fine texel + has the caster silhouette on the co-pack page, so it can **re-test that silhouette at the fine texel** for a near-pixel-perfect edge — walk stays coarse, only the raster goes fine. [F1](2026-07-24-shadow-edge-refine/forks.md#f1) refine-not-re-gather; [F2](2026-07-24-shadow-edge-refine/forks.md#f2) chose **(b) local re-walk in `LIGHT_FRAG` gated to `(0,1)` edge texels** (no gather change) over (a) store-caster-id. Distinct from the receiver clip (already fine). Rides [lightmap-resolution](2026-07-24-lightmap-resolution/README.md) + the co-pack. |
| [2026-07-24-light-prims](2026-07-24-light-prims/README.md) | blocked | **SUBSUMED by [2026-07-25-primitive-graph](2026-07-25-primitive-graph/README.md)** — lights are no longer *placed* at all, they are **carried** by a prim, so F1 (record wiring) and F2 (light-def band) dissolve; its **dirty design survives** (two `markDirty` entry points → `pendingRects`/`buildDirty`, cold/hot generalised) and moves there as P4. Kept for that design + the diagnosis below. **Make lights first-class world PRIMS** (user), so they're placed + auto-dirtied like things instead of injected into a debug array. Today lights are a bespoke `ShadowGather.this.lights[]` seeded by `seed()`/`__manylights()` and pushed to the bake by `buildLights()` gated on a **hand-set `coldDirty`** — nothing flows through the content/placement pipeline a tree uses (`WorldBridge → SquareCache.standingPrims() → tick → buildCasters`, auto free-list + `markPrimChange` cascade). Proven unusable this session: 50 lights pushed to `g.lights` never propagated (`lightCount` stuck at 3 — change-gated loop + no world home). Vocab ratified: a **primitive** presents as a **billboard** (sprite caster) or a **light** — per-presentation bands (`billboard_definition_data`+`billboard_data` ↔ `light_data`+future `light_definition_data`); the `prim_*`→`billboard_*` rename landed. Two pillars: **(1)** the light presentation is placed (writes `light_data`, its own record symmetric to `billboard_data`) + delivered through the primitive path; **(2)** two `markDirty` entry points (`markBillboardDirty` + new `markLightDirty`) cascade into `pendingRects → buildDirty`, cold/hot routing generalised, retiring the manual `coldDirty`/`forceColdDirty`/`lightsVer` flags. **Write-up only — blocked on B1** (F2 light-def band = the one remaining VARIABLES call; F1 resolved by the vocab). Unblocks every downstream effect (day/night, emissive, torches) that needs lights *in the world*. |
| [2026-07-24-lightmap-resolution](2026-07-24-lightmap-resolution/README.md) | open | **Cache per-light lighting at normal (`TEXTILE_SQUARE` 64/tile) resolution, for MANY lights** (user). Detail lives in lighting, not albedo, and the lightmap is 4× coarser than the normals it modulates. The coarse lightmap also **pre-sums lights into one averaged direction** — lossy (`Σ colorᵢ·N·Lᵢ ≠ (Σ colorᵢ)·N·L_avg`, [issues](2026-07-24-lightmap-resolution/issues.md#i1)). Fix: bake **`N·L` per light** into a fine, contiguous, cached (dirty-gated) lightmap — correct + amortized so many lights are affordable; blit collapses to `albedo × lightmap`. Crux = getting the fine normal into the contiguous bake without a `textile_slot` cross-family read ([forks F3](2026-07-24-lightmap-resolution/forks.md#f3)). Shadow stays coarse (upsampled); VRAM freed by the 4×→2× zoom cut. Converges with [world-space-lighting](2026-07-24-world-space-lighting/README.md) + normal-tilt. |
| [2026-07-24-world-space-lighting](2026-07-24-world-space-lighting/README.md) | open | **Light the 65° world, not the flattened screen** (user realization). Lighting math runs in screen space, but the world is a 65°-tilted plane — the N–S axis is foreshortened. One cause, three fixes: the **falloff** is a screen circle → should be a true-3D-distance **ellipse** (+ the light's height); the **normal `N·L`** dots a world-frame normal (from [normal-tilt](2026-07-24-normal-tilt/README.md)) against a **screen-space** light dir → mismatch; and the standing-prim front/back darkening falls out of a correct `N·L`. Fix = build the light→point vector once in **true world 3D**, use length for falloff + direction for `N·L`. **P0 = derive + simulation-check the trig BEFORE code** (the `2·tan65` class of risk). Behind a toggle, A/B vs screen-space. Builds on [world-geometry](2026-07-23-world-geometry/README.md). |
| [2026-07-24-map-compatibility](2026-07-24-map-compatibility/README.md) | open | **Investigation** (no feature): which maps may be cross-referenced by world coordinate, and which may not. On-prim casting broke twice by sampling the **`zdepth` composite** from the gather by world position; it drifts on zoom. Root cause: the composites are a **`textile_slot`** family (one variable-`slotPx` slot per tile in a fixed apron atlas, `slotPx` chosen by zoom — user's name), NOT the contiguous `textile_square` [`map-model.md`](2026-07-22-lighting-rebuild/map-model.md) claims. Rule that falls out: **never address a `textile_slot` map by recomputed world coord** — only via a cache-supplied UV (the display blit). Produces the map inventory + compatibility matrix, formalises `textile_slot`, and re-plans on-prim casting on a safe in-family source (the caster buckets). |
| [2026-07-24-shadows-onto-prims](2026-07-24-shadows-onto-prims/README.md) | open | **Attempt #3** at shadows-on-billboards. The reverted attempt's math (elevation `z = sin65·Δ`, projection to `G`, the seen-face + light-side cone culls, corridor↔brute bit-identity) was correct at a fixed zoom — only the INPUT was wrong (read the `zdepth` `textile_slot` composite by world coord → zoom drift). This stream feeds that SAME math from a **zoom-safe in-family receiver mask** ([forks F1](2026-07-24-shadows-onto-prims/forks.md#f1) — lean: bake a `textile_unit` receiver-depth map aligned with `shadow-cold`). Acceptance is a **zoom sweep**, not a fixed-zoom shot. Sidesteps the deferred map-compatibility F1. Supersedes `2026-07-23-shadows-on-prims`. |
| [2026-07-23-shadows-on-prims](2026-07-23-shadows-on-prims/README.md) | closed | **SUPERSEDED by [`2026-07-24-shadows-onto-prims`](2026-07-24-shadows-onto-prims/README.md).** Attempt #2 — built + reverted (zoom regression: read the `zdepth` `textile_slot` composite by world coord). Kept for its [`completed.md`](2026-07-23-shadows-on-prims/completed.md) — the detailed, correct-at-fixed-zoom gather math that attempt #3 reuses verbatim — and the [map-compatibility](2026-07-24-map-compatibility/README.md) root cause it drove out. |
| [2026-07-24-normal-tilt](2026-07-24-normal-tilt/README.md) | open | Bake the **world-tilt rotation** into standing-object normals correctly. Laigter emits normals in the **billboard frame** (card ∥ screen, flat = faces camera); the ground is tilted **65°**, so the field must be rigidly rotated **65° about the E–W axis** into the world frame (`Y_w = cos65·ny − sin65·nz`, `Z_w = sin65·ny + cos65·nz`). Today `bin/art`'s `_normal_tilt` does an **additive bias + Z-squash** (`ny -= 1.8; nz *= 0.18`) — over-pitches to ~84° AND distorts the relief. Fix = replace the bias with the rotation, keyed to one shared `WORLD_TILT_DEG` ([`shadowGather.ts:52`](../../client/webgl/src/game/viewport/shadowGather.ts)); retire `tilt_amount`/`tilt_z`; re-bake the corpus. [F1](2026-07-24-normal-tilt/forks.md) fork (decal / camera-facing / upright) resolved to **camera-facing (65°)**; per-facing sign table is the one open item. Builds on [world-geometry](2026-07-23-world-geometry/README.md). |
| [2026-07-23-lighting](2026-07-23-lighting/README.md) | open | Stand up the **lighting pass** (deleted in the 2026-07-19 nuke; G-buffer + shadows since rebuilt). Per display pixel: tile **presence** → accumulate each acting light (colour·intensity·falloff) → × **albedo**. Phased (user): **P1** emission (lights illuminate, flat), **P2** albedo+**normal** (Lambert diffuse — relief reads), **P3** **shadows** (mask each light by its `shadow-cold` u9 coverage — penumbra/umbra modulate). Dense point lights, not sun. First consumers: the 3-light debug rig. |
| [2026-07-23-incremental-presence](2026-07-23-incremental-presence/README.md) | open | **Deferred** CPU optimization: `buildPresence`/`buildCasters` scan all ~2000 in-window tiles/frame to rebuild-and-diff, though the GPU is already dirty-only (compare-write) and the dirty reach box is already known. A bidirectional **light↔tile index** (upsert-during-dirty + trailing prune for movers; full rebuild only on pan; gate buildCasters) drops per-mover CPU from O(window) → O(reach box). Working well at 3 lights; build when it scales. |
| [2026-07-23-penumbra](2026-07-23-penumbra/README.md) | delivered | **P0–P2 verified 2026-07-23.** Emitter-based **soft shadows** — make shadows soften with the light's `emitter_radius` + shadow stretch (real penumbra, not just edge AA). **P0 done**: shadow coverage u8→**u9** (512 levels; 14 high bits in the shadow texel's A reserve). Emitter multi-tap: penumbra (soft edges) + umbra (dark core receding with distance = base-dark → tip-light). u8→u9 coverage. 9-tap at display cap; 0 mismatches. |
| [2026-07-23-presence-in-data](2026-07-23-presence-in-data/README.md) | delivered | **P0–P3 verified 2026-07-23.** Fold **light-presence** + **caster-buckets** into the unified data texture as two more sets so they ride the SAME scatter pass — kills the second-FBO problem their separate textures created. Address on a **fixed region-sized torus** (1024×64 = 65 536 = one region's tiles; zone-strip fold; anchor-centred closest-wins) → deterministic, thrash-resistant (16-zone hysteresis), pan/zoom moves reads not writes; `map-model.md` at max rigidity. Self-address costs 1 of 8 slots (7 lights / 7 casters). Two-layer eviction (presence on region exit, prims/lights on zone eviction — reach gap makes it self-safe) + prim free-list. **Principle: DATA holds PERSISTENT state only** (dirty stays out). Planned; P0 = VARIABLES layouts. |
| [2026-07-23-unified-data](2026-07-23-unified-data/README.md) | delivered | **P0–P4 complete + verified 2026-07-23.** ONE 1024×1024 RGBA32UI **data texture** in 16 u16-addressable SETS (defs/prims/lights + constants; **records self-address** — u16 id in R's high half) fed by ONE 64×64 **command buffer** (v2.1 pure-payload fills: header of 16 u6 per-set counts + u8 opcode; replay-idempotent, rotating cursor) applied by point-scatter draws. CPU never uploads the data texture; gather binds 5 samplers; identity 0 mismatches, orbit at display cap. Post-mortems: ANGLE freeze + overlay stall (issues.md). |
| [2026-07-23-def-frame-anchors](2026-07-23-def-frame-anchors/README.md) | open | Rework `billboard_definition_data` onto the **whole-px-per-unit** model: pow2-square frames addressed by **u4 lod exponent** (+ u3 span, F1) instead of w/h; **minimum bbox in even units** + unsigned frame-relative offset; **u11 px nudge** (x-center, bottom-align); **3×3 frame anchors** (u2+u2) for placing the bbox on a prim's reported x/y. Kills fractional px-per-unit at the type level, frees bits, generalizes anchoring. P0 = ratify the amended layout; P5 (size off the DSL) user-deferred. |
| [2026-07-22-lighting-rebuild](2026-07-22-lighting-rebuild/README.md) | delivered | **Full teardown + clean re-implement** of the billboard-quad shadow system — **P0–P6 complete + verified 2026-07-23**: data textures (light pos/colour/reach, per-tile ≤8 lights + ≤8 casters), **geometric point-in-quad** occlusion at every zoom, **opaque-bbox** quads, **silhouette-textured** shadows (surface coverage, in-range by construction), **scoped dirty routing** for moving lights, and the **corridor re-earned bit-identical to brute force**. Open follow-ups live in its forks: [F1](2026-07-22-lighting-rebuild/forks.md#f1) LOD reconciliation, F2/C5 multi-page silhouettes. Supersedes `2026-07-22-shadow-corridor` (archived → `~/archive/shadow-corridor/`). |
| [docs-authority](docs-authority/README.md) | open | This system: front-door index + `docs-check`/`work-check` audits + the enforcement hooks. |
| [2026-07-21-showrt](2026-07-21-showrt/README.md) | open | Port the `/showRT` render-texture preview panel onto `client/webgl` — the one remaining W4f debug command ([`webgl-engine`](webgl-engine/README.md) descoped it: "needs a GL→DOM readback"). The data source (`Viewport.renderTextures()`) + `/overlayRT` already exist; missing is the panel + the GL→DOM transport. Recommended: **one grid `RenderTarget` composited in the viewport ctx + a single throttled `gl.readPixels` → a DOM `<canvas>`** (fewest stalls), on a `DomPanel` shell (native scroll drops pixijs's hand-rolled scrollbar). |
| [2026-07-21-shadow-bitfield](2026-07-21-shadow-bitfield/README.md) | open | Move the shadow cast onto a **world-space toroidal `shadow-cold` `RGBA32UI` bitfield** (each cold light = one bit), rebuilt by a **dirty-rect gather** — one fragment pass per grouped rectangle loops the reaching lights, box-culls, tests shadow, ORs their bits (**no ping-pong**). Factors a shared **rect-accumulation** helper (`SquareCache` can adopt). `usampler2D` per-bit `/overlayRT` debug decode. The per-light **mask** lighting consumes next. **Supersedes `shadows`; retires hot-shadows F2 ping-pong.** |
| [hot-shadows](hot-shadows/README.md) | blocked | **TABLED 2026-07-21** (hot lights need more thought). The **hot (dynamic) tier**: hot lights + prims via **uniform** (same packed format as cold textures), a **work-item** uniform replacing the per-light LUT run, per-frame **budgeting**. The bitfield-output half moved to **`2026-07-21-shadow-bitfield`** (cold-only, gather not ping-pong). Resume after the bitfield lands. |
| [cold-data-textures](cold-data-textures/README.md) | done | Move cold lights + casters off per-frame instance attrs into persistent `RGBA32UI` **data textures** (light → LUT → prim indirection) read by the shader via `texelFetch` — no per-frame cold upload. The `caster-lut` C5 data layer; unblocks `shadow-projection` P5 (rotation in the LUT). Layouts in `VARIABLES.md`. |
| [shadow-projection](shadow-projection/README.md) | open | Align the webgl `ShadowCaster` with the **5-triangle projected-silhouette** model (`design/shadows.md` + `bin/shadow-projection-sandbox.html`), built **on the GPU** — an instanced vertex shader projects a θ-tilted billboard per-corner from per-instance caster/light data (two-regime facing, silhouette-derived depth, alpha-masked). Replaces the analytic trapezoid ([webgl-engine D-2](webgl-engine/deviations.md)) + delivers `caster-lut` C5 / W7; feeds the `shadows` screen-hot generator. |
| [webgl-engine](webgl-engine/README.md) | open | **Retire PixiJS** — a new adjacent client (`client/webgl`) on a bespoke WebGL2 renderer. We use a thin slice of Pixi (render backend + Sprite/Graphics/Text) and it's exactly the slice blocking ES 3.00 (integer textures / MRT / VTF all hit Pixi walls). Copy the non-Pixi code (DOM UI, atlas, DSL, sync, shaders), rewrite the render seams ourselves, reach parity, retire Pixi. `caster-lut` C5 becomes W7 (clean, on the owned engine). |
| [shard-tables](shard-tables/README.md) | open | The `*_tables!` generalization: fold every composing shard into generic table macros; all writes via events. P1–P3 live, P4 pending. |
| [art-metadata](art-metadata/README.md) | open | Per-variant `meta.json` sidecar from `bin/art` (channel tints + shadow outline). P1–P2 done. |
| [shadow-tiered](shadow-tiered/README.md) | done | The shadow rework fixing `shadow-world`'s aliasing: cast the realtime lights in **screen space** (window-bounded) → `screen-shadow-*`, merge into the **world-space** `shadow-*` bitfield (clear dirty bits + OR the remapped screen field, one pass), display `shadow` OR `screen-shadow`. **T1–T7 verified 2026-07-20** — no aliasing on pan, moved lights re-cast same-frame; the stale-on-pan slot-invalidation (I-9) fixed during verify. Graduates toward **shadows**. |
| [mrt-bakes](mrt-bakes/README.md) | done | Collapse the four G-buffer bake passes (`albedo`/`surface`/`normal`/`zdepth_world`) into **one MRT pass** — ~4× fewer bake draws. One ES 3.00 shader with `layout(location=0..3) out` into a `RenderTarget({colorTextures:[...]})` (MRT is Pixi-native; ES 3.00 multi-out is the enabler). Real work = merging the four bake shaders into one fragment (shared `discard`, tier branch). Behaviour-preserving. **B1–B5 verified 2026-07-20**: one merged ES 3.00 fragment (4 outs) + a 4-attachment `RenderTarget` scratch (manual `gl.drawBuffers` — Pixi omits it); all four channels pixel-identical. Dead per-channel code cleanup spawned as a follow-up. |
| [es300-migration](es300-migration/README.md) | done | Migrated **every** client shader off Pixi high-shader (ES 1.00) onto raw **ES 3.00** — one standard. **M1–M5 verified 2026-07-20.** Linchpin collapsed to a one-bit `#version 300 es` wrapper (`compileHighShaderGlProgramES300`) — Pixi's templates are version-agnostic, so its camera plumbing rode along; no hand-rolled builder ([D-1](es300-migration/deviations.md)). All 9 programs swapped + verified identical; `grep compileHighShaderGlProgram` is clean. Integer-texture / MRT wins now uniform follow-ons. |
| [es300-hello](es300-hello/README.md) | done | Prove a hand-written raw **GLSL ES 3.00** shader compiles + renders in the client (everything else is high-shader ES 1.00). **E1–E3 verified 2026-07-20**: a `/es300` full-viewport checkerboard from `uint`/bitwise (syntax ES 1.00 can't compile) renders, coexisting with the ES 1.00 scene. Finding: **Pixi v8 handles ES 3.00 natively** (just put `#version 300 es` in the fragment). Foundation for `caster-lut` C5's vertex-texture-fetch GPU cast. |
| [caster-lut](caster-lut/README.md) | open | Shadow **casters** + per-light **in-range cull** in textures (`1024×12` RGBA32F: prim data, light + `start_index`/`count`, a 49,152-entry **LUT**). **C1–C4 done** (indirection drives the cast off CPU mirrors). **C5 remaining** = the GPU payoff, now planned as 3 coupled facets: an ES 3.00 instanced cast (`texelFetch`/VTF) + switching the shadow bitfield to **integer textures** (`uint`, retires float-mod, lifts the 24→32/128-bit ceiling) + the cast becoming a shader (integer RTs kill blend). |
| [light-data-texture](light-data-texture/README.md) | done | Feed per-light data to the shader through a **5×5 `RGBA32F` data texture** (one column per light) instead of uniforms/hardcoded colours — the mechanism dense many-lights needs. **L1–L4 verified 2026-07-20**: shadow colour read from the texture, re-rolled once/sec → per-light colour change proves the live JS→texture→shader path (float worked; ES 1.00 ruled out true `u32`). Extends `shadow-tiered`; L5 (position from texture) deferred to **shadows**. |
| [shadow-world](shadow-world/README.md) | closed | Proved world-space `shadow-cold` storage + `/overlayRT` decode (W1–W5), BUT its direct world-cast **aliased off-window lights** (raw `mod`, no window-bounds check) — surfaced on pan. Superseded by **shadow-tiered** (screen-cast + copy). Kept for the mapping + overlay-decode lessons. |
| [shadows](shadows/README.md) | closed | **Superseded 2026-07-21** by [`2026-07-21-shadow-bitfield`](2026-07-21-shadow-bitfield/README.md) (per hot-shadows F3): the world-space per-bit **dirty-rect gather** replaces this stream's screen→world 4-copy — the per-bit separation makes world-space viable, so the screen-space detour is no longer the plan. Kept for the toroidal-wrap + `/overlayRT` decode lessons. |

**Archived out-of-repo** — completed/reverted streams live in **`../archive/`** (git history retains them
at their last in-repo commit). **2026-07-20:** `bitfield-rt` (done — bitfield RT round-trip + ping-pong)
and `shadow-cast` (done — shadow-casting into a bitfield + incremental updates), which the new
`shadow-world` builds on. **2026-07-19** (`../archive/work-stripped-2026-07-19/`): `texture-restructure`,
`spacetime-again`, `coord-purge`, `cold-rework` (→ **shard-tables**), `docs-migration`, `prim-batching`,
and `lighting` (nuked; restart is **shadows**, target in the pixijs component `intent`/`design`).

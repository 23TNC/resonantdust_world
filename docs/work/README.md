# Work — index

_The flowing execution state. Each `work/<w>/` turns component `plan`s into executable items and
spans components by nature. Convention: [`../CONVENTIONS.md`](../CONVENTIONS.md). Last updated:
2026-08-04._

**Status** = the stream's lifecycle, not the session's focus: `open` (in progress) · `done`
(delivered; may have a small hand-off) · `closed` (abandoned/refuted) · `blocked` (needs input).
The continuation hook's notion of the **active** stream is *session-scoped* (which open stream this
session is driving — inferred, not a field here); see [`docs-authority`](docs-authority/README.md) P5.

| Stream | Status | What it is |
|---|---|---|
| [2026-08-07-logs-drop](2026-08-07-logs-drop/README.md) | open | **A felled tree leaves LOGS where it stood** (user, 2026-08-07: "implement the log things drop when trees are felled") — the successor lumberjack reserved by name (its F5/I9). `yields = "logs"` is authored on the CARRIER BINDING beside `magnitude` ([F1](2026-08-07-logs-drop/forks.md#f1) — the tree yields, shrub/cactus still fell to nothing; an interaction-level yield would make shrubs drop logs); the worker's destroy composer swaps its ONE clearing SET's kind operand from `0` to the logs `kind_reference` — a REPLACE, no clear-then-place pair ([F2](2026-08-07-logs-drop/forks.md#f2)); `logs` = ordinary scatter with a PLACEHOLDER tint and NO interactions ([F3](2026-08-07-logs-drop/forks.md#f3) — hauling/inventory is the recorded successor, [I5](2026-08-07-logs-drop/issues.md#i5)). Watch-items: the binding accessor's shape change sweeps worker + wasm + golden in ONE move ([I1](2026-08-07-logs-drop/issues.md#i1)); the registry append needs all four sim crates rebuilt (lumberjack I11's law, [I2](2026-08-07-logs-drop/issues.md#i2)); the yielded override must never read as a tombstone ([I3](2026-08-07-logs-drop/issues.md#i3)). Exit: fell a tree → logs at its cell (sql + render + reload-persist), a felled shrub still clears, cold boot; user's eyes close. |
| [2026-08-07-lumberjack](2026-08-07-lumberjack/README.md) | done | **The lumberjack trait, adjacency, the intent queue, felled trees** (user, 2026-08-07: "add a lumberjack trait… an interaction to cut down trees… the affordance of pawns with the lumberjack trait adjacent to the tree… update the drink interaction to also require adjacency… implement an intent queue… a vector of intents per pawn… maximum of 5… track how many tics we've spent conducting an action… the cut down tree interaction will remove the tree after the interactions number of tics has passed… apply this same interaction to other similar tree like objects"). Everything through the four-family model: `lumberjack` = a leveled trait into a new `logging` stat, gated by a `can_fell_trees` affordance ([F7](2026-08-07-lumberjack/forks.md#f7)); adjacency = a third `location` rule `"adjacent"` (Chebyshev ≤ 1 INCLUSIVE — keeps the wolf's stand-on-water drink green, [F2](2026-08-07-lumberjack/forks.md#f2)); the INTENT QUEUE = an EPHEMERAL per-pawn list at the worker (user [F1](2026-08-07-lumberjack/forks.md#f1): the in-flight queued EVENT is the durable half; cap 5, fresh order REPLACES — the one-chain law lifted a level, [F3](2026-08-07-lumberjack/forks.md#f3)) that composes `[move_to(adjacent), act]` at the WORKER (one composer, npc/menu cannot drift); `duration` rides queue_at (completion at `+N`) and EVERY completion RE-VALIDATES affordance + location — stale intents resolve to logged NO-OPS (the corpus's RESERVED I9 seam, consumed here). `cut_down` (`duration = 30`, `destroy = "carrier"` → the proven cold-overlay `SET … TYPE_BIOME_THING … 0` — [F5](2026-08-07-lumberjack/forks.md#f5)) on tree/shrub/cactus ([F6](2026-08-07-lumberjack/forks.md#f6)); humans author lumberjack level 1 ([F4](2026-08-07-lumberjack/forks.md#f4)), wolves refuse by the gate. Named successor, NOT built: `yields = "logs"` places a thing where the tree stood ([I9](2026-08-07-lumberjack/issues.md#i9)). Exit: a fresh lumberjack walks-then-chops a far tree through the pie menu, the tree AND its shadow leave the render, shrub/cactus fell, wolf refused, drink-from-shore, cold boot; user's eyes close. _DELIVERED 2026-08-07 (13/13), the last arc on a USER-REQUESTED CLEAN WORLD (`redeploy --force`): the far chop executed AT its fire tic (`destroyed=true version=1`, tombstone in the thing overlay, render clear), the RACE logged `intent completion NO-OP` for the loser (the user's law verbatim), the wolf's tree menu EMPTY, tree ×3 + shrub felled (cactus authored-identical, undrilled — no desert in reach), thermostat + composed 17-tile walk-then-drink beside. The first REMOVE drill exposed + fixed two edge bugs (thing tombstones framed `removed:true`; the process-memory seed guard re-seeding zones over tombstones — I10) and a standup gotcha (stale master silently skips the registry seed — I11). Three cold boots survived, incl. a host bluescreen; a mid-chop worker bounce dropped exactly the ephemeral pending tail (F1 observed). Details in completed.md._ |
| [2026-08-06-human-pawns-redux](2026-08-06-human-pawns-redux/README.md) | done | **Human pawns re-implemented — the two-prim pawn lives on the current stack** (user, 2026-08-06: "re-implement the human pawns… multiple prims currently one for the head and one for the body"). The audit (in the README): the original 2026-07-30-human-pawns SKELETON survived every rework — two-part corpus (body slot 0 / head slot 1 with `part=1`, `depth=1.0`, `offset.z=0.87`), complete art (9 bodies × 16 heads per kind, per-facing masters), the payload `PART` opcode intact through the stat-model reshape, per-slot rendering with `carrierOf` — but FOUR things rotted: NO mint path exists (`part_entry` has zero callers; the P4 fixture's console mover died with `moveEntity`), the authored mover SUBFRAMES are dead data ([subframe-ingest I11](2026-08-02-subframe-ingest/issues.md#i11) still open — never registered), drink would be OFFERED-then-REFUSED (humans author no `needs`), and stale paper (pawn-part-placement's plan targets deleted `pawns.rd` knobs; the retired `layer` lane still written). The plan: repair first ([F6](2026-08-06-human-pawns-redux/forks.md#f6) — I11 registration per slot; `needs = ["thirst"]`, [F4](2026-08-06-human-pawns-redux/forks.md#f4)), then a `spawn` CHAT command as the mint door ([F2](2026-08-06-human-pawns-redux/forks.md#f2)), per-pawn body/head VARIANTS via the PART refs' variant nibbles ([F3](2026-08-06-human-pawns-redux/forks.md#f3) — one def, u4 appearance), humans driven by the pie menu (move at derived 24 t/t, drink satisfying), pawn-part-placement CLOSED as superseded ([F5](2026-08-06-human-pawns-redux/forks.md#f5), lanes carried in [I9](2026-08-06-human-pawns-redux/issues.md#i9)). Exit: two visibly different humans minted by chat, menu-driven arcs, wolf drills unchanged, cold boot; user's eyes close. _DELIVERED 2026-08-07 (13/13): three humans minted by `/spawn` (heavy body-5/head-12 female beside slim default + broad male-b2/h7 — variant nibbles verbatim in sql), I11 registration live (authored crops verified in the resolver), menu-driven move at the DERIVED 24 t/t + drink `satisfied=Some("thirst")` +3, the north depth-flip proven in warm-prim z, wolf thermostat/trips/panel unchanged beside them in one session; details in completed.md._ |
| [2026-08-06-input-rework](2026-08-06-input-rework/README.md) | open | **Active selection, the pie menu, movement as an interaction** (user, 2026-08-06: "This active selection will move to right click. Left click will become a context sensitive pie menu… display available interactions the actively selected object has with the left clicked object… rounded rectangles around the cursor's x/y… menu_text 'Move To'… Finally we will replace our existing move events with our new interaction based movement system"). The stat-model's declared consumer: RIGHT click takes the whole current selection behavior (panel/outlines); LEFT click asks "what can my active pawn do with THAT" through the SAME `interaction_available` + carrier bindings the worker enforces ([F1](2026-08-06-input-rework/forks.md#f1)); options render as `menu_text` rounded rects at a fixed radius, equal angles, ONE menu, any input dismisses ([F7](2026-08-06-input-rework/forks.md#f7) — DOM overlay, empty set shows nothing). **Movement**: `[[interaction]] move_to` (gated `can_move_ground`, location `"target"` — the clicked tile is the carrier, [F4](2026-08-06-input-rework/forks.md#f4)) with the new `move` effect; ground tiles carry it, walls DON'T (soft passability by authoring, [F9](2026-08-06-input-rework/forks.md#f9)); `MOVE_TO` goes WORKER-ONLY — the chain, supersession, and the speculation intent channel survive untouched ([F3](2026-08-06-input-rework/forks.md#f3)/[I1](2026-08-06-input-rework/issues.md#i1)). **Speed = the derived `ground_speed` everywhere** (worker spacing, npc deadlines, client glide); the `speed` field + speeds tables + the I10 guard DIE ([F8](2026-08-06-input-rework/forks.md#f8)). A RESERVED input vocabulary (`pawn`/`destination`/`amount`) makes the menu generic — drink re-authors to two inputs with its need BAKED ([F5](2026-08-06-input-rework/forks.md#f5)). Deleted with the door: `move_to_program`/`Command::Move`/`moveEntity` and the legacy `wildlife` brain ([F10](2026-08-06-input-rework/forks.md#f10)). **DELIVERED 2026-08-06** — real-mouse drills: right-click selects (pawn silhouette + panel with the DERIVED `12 tics/tile`), the water menu offers Move To (Drink joins only while STANDING on it — the location rule live), a rect click walked the wolf to (102,68) with the glide, Drink sipped `18.48 → 21.48` + quenched, two rapid orders left ONE chain, and the npc's thermostat arcs ran beside it. Found live, all fixed: the npc's drink filter vs universal carriers ([I11](2026-08-06-input-rework/issues.md#i11)), the menu under the panel z-bands + off-cursor under #app's transform, and the edge-door drill FAILING OPEN on the docker/WSL2 mtime miss (the redeploy's edge build silently skipped `ws.rs`). User's eyes close. |
| [2026-08-06-stat-model](2026-08-06-stat-model/README.md) | open | **The gameplay model generalizes — stats, packed rows, predicate affordances** (user, 2026-08-06: "we add 'stats' to our list of stuff our pawns can 'have'. So… stats, needs, traits, conditions. The trait 'Walks' will add N to the pawns 'ground_speed' stat… the affordance is something like 'Can Move Ground', which will check 'ground_speed > 0'. Then the interaction can have the affordance"). Reworks what 2026-08-06-interactions delivered, and comes FIRST — the input stream (active selection, pie menu, interaction-based movement; recorded, not yet opened) builds on it. A pawn HAS four families ([F1](2026-08-06-stat-model/forks.md#f1), user): TRAITS (leveled stat contributors, `kind:12\|variant:4\|level:16`, minted at CREATE), CONDITIONS (`remaining_at_write:16` + written tic — backward-looking, never a forecast, [F3](2026-08-06-stat-model/forks.md#f3) user), NEEDS (u16 FIXED-POINT over the authored domain in a NEW sub-table `(entity, packed, set_tic)` so need writes fan ALONE and lazy depletion survives, [F2](2026-08-06-stat-model/forks.md#f2)/[F4](2026-08-06-stat-model/forks.md#f4)), and STATS — derived only, never stored, never fanned ([F8](2026-08-06-stat-model/forks.md#f8)). Affordances hang off INTERACTIONS as structured stat PREDICATES (`can_drink` ⇔ `metabolism > 0`; `variants`/`requires` die, [F5](2026-08-06-stat-model/forks.md#f5) user); carriers bind `interactions = [{name, magnitude}]` ([F9](2026-08-06-stat-model/forks.md#f9)). ONE combiner (max-of-mins/min-of-maxes, authored winner, global bounds — stats AND needs, [F6](2026-08-06-stat-model/forks.md#f6) user) + the RE-STAMP law ([F7](2026-08-06-stat-model/forks.md#f7): every modifier mutation re-stamps affected need rows; expiries integrate piecewise). Walks/`ground_speed` authored and derived here; movement rewires in the input stream ([F12](2026-08-06-stat-model/forks.md#f12)). **DELIVERED 2026-08-06** — the wolf drinks on packed rows: sips of exactly +3.0 with quenched's ×0.5 VISIBLE between them (0.0138/6 tics = the halved rate), the piecewise crossing prediction coming true, the panel's cards rendered entirely from the fanned payload + `needs` rows, and a cold boot whose adopted wolf woke Thirsty off a row that had LAZILY DRAINED across the downtime. Found en route: the re-stamp duty belongs to COMPOSERS ([I12](2026-08-06-stat-model/issues.md#i12) — modules hold no corpus; the eval hardened for lone late grants) and the combined seed+grant drill race (deferred). User's eyes close. |
| [2026-08-06-interactions](2026-08-06-interactions/README.md) | open | **Traits, interactions, affordances — the pawn learns to DO** (user, 2026-08-06: "define traits, interactions and affordances in toml… An interaction is something a pawn can do… An affordance defines what interactions are available to the pawn… drink 3 to satisfy 3 thirst"; revised the same day by the user's seven planning answers). The declared successor of needs-moodlets — `quenched`'s TIMED lane finally gets its real producer. **One id system** ([F1](2026-08-06-interactions/forks.md#f1), user): every gameplay def — needs and conditions INCLUDED — authors a taxonomy (`type = "gameplay"`, subtype = category, kind, variant `default`) and the REGISTRY numbers it like things; `needs.toml`'s explicit `id = N` law dies, which forces the payload consequence faced head-on ([I1](2026-08-06-interactions/issues.md#i1) — u32 ids outgrow the 8-bit payload lanes, NEED/CONDITION entries restructure, dev wolves re-mint). **Values are float32** ([F3](2026-08-06-interactions/forks.md#f3), user): event words carry f32 BIT PATTERNS; each need authors its OWN min/max domain ([F7](2026-08-06-interactions/forks.md#f7), user — thirst provisionally `0..100`, so "drink 3" = +3.0 of a 100-full scale); decimals make percentages and gain/loss rates first-class, and signed effects make damage-a-need the same mechanism. **Execution is a queued EVENT** ([F4](2026-08-06-interactions/forks.md#f4), user): `EXECUTE_INTERACTION [interaction_id, version, input_count, inputs…]` through the edge queue; the WORKER (corpus + gameplay manifest) resolves the def's input signature ([F5](2026-08-06-interactions/forks.md#f5)), checks the pawn stands ON a carrier tile ([F8](2026-08-06-interactions/forks.md#f8) — adjacency/`unit.x/y/z` + "move to and execute" are the recorded later generalization), and lands effects through the ONE `needs_eval` + the existing SET_NEED/GRANT_CONDITION splices. The affordance ([F2](2026-08-06-interactions/forks.md#f2) — binds trait→interaction ONCE, carriers reference by name + magnitude) is the npc's AVAILABILITY check this turn; deeper authority later ([I4](2026-08-06-interactions/issues.md#i4)). **DELIVERED 2026-08-06** — the arc ran UNPROMPTED: Thirsty card → walk to (102,68) → +3.0 sips off the lazily-depleted value → Thirsty cleared → **Quenched +0.20** on the panel, the wolf re-sipping when depletion dipped it back (a working thermostat). Two defects found live and fixed: the `queue_at(t+1)` TIC_GAP silent loss ([I11](2026-08-06-interactions/issues.md#i11) — BUILD_WALL had it too) and the future-stamped-row phantom (an eval half-window guard). User's eyes close. |
| [2026-08-04-conditions](2026-08-04-conditions/README.md) | done | **Moodlets become CONDITIONS, and the panel shows them as cards** (user, 2026-08-04: "rename moodlets to feelings… display feelings as cards… the 4 highest priority maximized displayed horizontally along the bottom of details", then the correction that reframes the model: *"moodlets can affect more than feelings, for example needs. So instead of renaming to feelings lets rename to conditions"*). The rename goes all the way down — corpus keys, loader types, the ONE eval, the codec payload op, the action constant, the pawn module's **reducer** (a published schema, [I1](2026-08-04-conditions/issues.md#i1)), both binding trees, npc, client, and the three authoritative docs — with **no wire value moving** ([F1](2026-08-04-conditions/forks.md#f1): opcode 11, payload op 3, ids pinned; the golden fixture is the proof it is name-only). The new word forces the old sub-vocabulary out: a "conditional moodlet" becomes a **derived** condition, a stored grant a **timed** one ([F4](2026-08-04-conditions/forks.md#f4)). The user's correction is preserved rather than built: `mood` becomes ONE named effect field with room beside it, not the definition of a condition ([F6](2026-08-04-conditions/forks.md#f6) — the effect table is the successor's charter, alongside the still-pending drink action). Priority is **authored corpus data**, not derived from `\|mood\|` ([F2](2026-08-04-conditions/forks.md#f2) — derive-from-mood cannot say "mild but urgent" and means nothing once a condition's effect is a need), sorted in the shared eval so npc and panel agree ([F3](2026-08-04-conditions/forks.md#f3)). `PANEL_CSS.overflow: hidden` clips every child at the panel border, so a card strip **cannot** exceed the details width from inside it ([B1](2026-08-04-conditions/blockers.md#b1)) — the user's answer: make the strip a **sibling** of the panel, a fixed-position element on the panel's host, anchored off `rectChange`, in BOTH states so there is no reparent at the click ([F7](2026-08-04-conditions/forks.md#f7)); the clamp moves out to the VIEWPORT edge — and slides the strip left before it scrolls, so a right-docked panel still shows every card ([F9](2026-08-04-conditions/forks.md#f9)). **Delivered 2026-08-04**, 34/34: the drill's wolf crossed thirsty→dehydrated at tic 1962 against a prediction of 1958 (one decision tick), panel and npc agreeing, cards flipping on screen. Three defects fixed en route, none of them this stream's: the golden fixture could no longer be re-blessed ([I2](2026-08-04-conditions/issues.md#i2)), `rd build core --check` had been red since 2026-08-03 ([I3](2026-08-04-conditions/issues.md#i3)), and a two-week-old edge container predated its own `content` bind mount ([I4](2026-08-04-conditions/issues.md#i4)). Awaiting the user's eyes. |
| [2026-08-05-content-packages](2026-08-05-content-packages/README.md) | done | **The corpus is a TREE; a folder is a mod** (user, 2026-08-05: "recursively include toml… so we can create packages that effectively function as mods"). `content/**/*.toml`, no manifest, no registration, no load-order file ([F1](2026-08-05-content-packages/forks.md#f1)) — possible only because the streams before it removed every reason the corpus needed structure: ids come from the REGISTRY not corpus position, the taxonomy is authored, and `(taxonomy, version)` uniqueness makes two packages claiming one definition a **load error by construction** rather than silent last-writer-wins. Six read sites ([I1](2026-08-05-content-packages/issues.md#i1)), of which two are not mechanical: `content_version` hashes the **BASENAME**, which becomes a collision the moment paths nest (`mods/a/things.toml` and `mods/b/things.toml` hash the same, and MOVING a file would not change the fingerprint at all, so hot-reload never fires) — fixed by hashing the relative path ([F3](2026-08-05-content-packages/forks.md#f3)); and the client's offline embed hard-codes four `?raw` imports, needing `import.meta.glob`. The one real decision ([F2](2026-08-05-content-packages/forks.md#f2)): server-only content is filtered by **what the data IS**, not what the file is called — today's `n != "biomes.toml"` is trivially defeated by `mods/foo/my-biomes.toml`, which would ship worldgen rules to every client silently. Override semantics deliberately OUT of scope ([I3](2026-08-05-content-packages/issues.md#i3)): a mod changes a def by authoring a new VERSION of it, which already coexists and wins resolution; replacement needs precedence rules nobody has asked for yet. **DELIVERED 2026-08-05** — a drill package registered end to end (registry 182→183, `moonpetal` → `0x20000010` in the browser) and **neither** biome file reached the client, including the package's own. Found the SECOND and THIRD private copies of a content reader ([I4](2026-08-05-content-packages/issues.md#i4), [I5](2026-08-05-content-packages/issues.md#i5)) — the edge's own `content_version` still hashing basenames, and the GOLDEN's own flat `read_dir`, which had gone blind to packages and would have kept passing while a mod added definitions it never checked. Both found because a test that should have broken didn't. |
| [2026-08-04-definition-registry](2026-08-04-definition-registry/README.md) | done | **The corpus describes, the server numbers** (user, 2026-08-04: "if we just add type subType and kind to our toml, they self describe" · "I see no reason to even hold numbers"). The wire id is ALREADY the taxonomy (`type:4|subtype:12|kind:12|variant:4`) but the corpus authors only `id = N` and the other three coordinates are recovered by **string-parsing an art path** through a code-owned palette — a definition's identity derived from where its pictures live. So: the corpus authors `type`/`kind` plus APPLICABILITY ARRAYS over `subType`/`variant` ([F2](2026-08-04-definition-registry/forks.md#f2) — one conifer block covers 48 tuples, not 48 blocks), a server-owned spacetime table allocates a `u32` id per tuple, and clients resolve `string→id` locally ([F9](2026-08-04-definition-registry/forks.md#f9)). **Supersedes [toml-content F1](2026-08-04-toml-content/forks.md#f1)** — explicit ids were right while the LOADER owned identity. Versioning is the point: any change mints a new id, `max(version)` wins for NEW placements, and existing objects keep old ids **forever** ([F6](2026-08-04-definition-registry/forks.md#f6)) — the user's apple case (new apples expire in 1 h, old in 2 h; what of one with 30 min left?). The id stays STRUCTURED, forced not chosen ([F3](2026-08-04-definition-registry/forks.md#f3)): three hot paths unpack its bits and the worker's `CREATE` type-routing is a sim safety property ([I1](2026-08-04-definition-registry/issues.md#i1)). Version bumps burn KIND space, not subType, for isolation ([F5](2026-08-04-definition-registry/forks.md#f5)); reclaim is designed but NOT built — `entity_state_log` is append-only history and a reclaimed id makes replay lie ([F7](2026-08-04-definition-registry/forks.md#f7)). Deletes the stem-parsing, the species palette, and `is_append_compatible_with`. **The packed layout is FROZEN** ([F13](2026-08-04-definition-registry/forks.md#f13)) — this stream changes no data structure; 16 variants per kind is a constraint the allocator enforces, not a problem to solve. All blockers answered: the taxonomy is uniform 4 axes and `white` is the no-art fill ([I4](2026-08-04-definition-registry/issues.md#i4)); subtype STAYS in the def ([F10](2026-08-04-definition-registry/forks.md#f10)); version bumps on simulation-visible change only ([F12](2026-08-04-definition-registry/forks.md#f12)); the registry lives in `index` with the single master allocating ([F11](2026-08-04-definition-registry/forks.md#f11)). |
| [2026-08-04-content-toml-only](2026-08-04-content-toml-only/README.md) | done | **`content/` holds only the authored TOML corpus** (user, 2026-08-04: "move non-toml out of content"). The follow-on to `toml-content`, which left one deliberate exception — and the inventory found six more paths nobody had counted ([I1](2026-08-04-content-toml-only/issues.md#i1)): the three generated ART manifests (`visual/manifest/*.rd`, written by `bin/art`, read by **nobody**), the R2 corpus index, and the four `servers/<env>` routing files. The rule: a tracked file under `content/` is TOML a human wrote to describe the world; a generated index belongs beside the tree it indexes ([F1](2026-08-04-content-toml-only/forks.md#f1) — `textures/manifest/`, untracked as `textures/` is) in the format its machine readers want ([F2](2026-08-04-content-toml-only/forks.md#f2), JSON), and deploy topology belongs to deploy ([F3](2026-08-04-content-toml-only/forks.md#f3), `deploy/servers/<env>`, columns intact — bash reads it). The count exposed three live defects: the R2 content path **404s** on five keys deleted last stream ([I2](2026-08-04-content-toml-only/issues.md#i2) — fixed by deleting the index, not refreshing it: the edge lists the bucket, [F4](2026-08-04-content-toml-only/forks.md#f4)), `def_span.py` resolves **zero** stems so fresh texture leaves lose their packing-square stamp ([I4](2026-08-04-content-toml-only/issues.md#i4)), and a **fourth** private content walk survives in the edge still speaking `.rd` ([I5](2026-08-04-content-toml-only/issues.md#i5)). Ends with a `rd content-check` gate in pre-commit so it stays true, and carries the destination forward: **one** texture index, not two ([F5](2026-08-04-content-toml-only/forks.md#f5)). **DELIVERED 2026-08-04** — `content/` is the five corpus TOMLs and a pre-commit gate keeps it that way. En route: the `/content` wire key `rd` → `toml` and `bin/dsl` → `bin/content` ([F6](2026-08-04-content-toml-only/forks.md#f6)/[F7](2026-08-04-content-toml-only/forks.md#f7)), proved by recolouring grass and watching the live client repaint; and **7 edge worldgen tests found red** since the DSL died ([I8](2026-08-04-content-toml-only/issues.md#i8)) — no command in this repo runs every crate's tests. P5's merge is documented but **not built**: measuring refuted its premise ([I9](2026-08-04-content-toml-only/issues.md#i9)) — variants/parts are already stems, `subkinds` is empty everywhere — and `tex_manifest` is disk-only, so the generator is still the only planned R2-mode index ([F8](2026-08-04-content-toml-only/forks.md#f8)). |
| [2026-08-04-toml-content](2026-08-04-toml-content/README.md) | open | **The DSL retires; objects become TOML data** (user, 2026-08-04: "phase out our DSL for now, and implement our objects using toml… our game is not benefiting from the full DSL… our objects are almost entirely data definitions"). Measured first: the ~1,030-line corpus uses FOUR features — `&path set` constants, `^prim call` parts, registry `@define`s, and the biome classifier — and only the last is behavioral, with a perfectly regular shape (threshold conjunctions + ordered salted scatter) that encodes as declarative rules ([F3](2026-08-04-toml-content/forks.md#f3), `gte/lt/lte` keys mirroring the ops exactly). **The `Bundle` is the seam** ([F2](2026-08-04-toml-content/forks.md#f2)): every consumer keeps its accessor surface; only the loader swaps — which is also the "for now" insurance. **Ids go EXPLICIT** ([F1](2026-08-04-toml-content/forks.md#f1) — they are STORED data: zone kinds, pawn defs, need payload words; the loader refuses duplicates), pinned to today's positional values. **Nothing swaps until byte-identical** ([F5](2026-08-04-toml-content/forks.md#f5)): a golden fixture (every table + a worldgen grid + needs probes) gates the cutover, then parser/vm/`.rd` are DELETED and the crate renames to `shared/content` ([F4](2026-08-04-toml-content/forks.md#f4), last, as pure mechanics). One def = one record; the `:data`/`:visual` facet split dies with the hooks ([F6](2026-08-04-toml-content/forks.md#f6)). Exit = an identical-looking world + the standing drills (wolf trip, thirst crossing, panel moodlets) green on TOML; user's eyes close. |
| [2026-08-03-needs-moodlets](2026-08-03-needs-moodlets/README.md) | open | **Needs hidden, moodlets shown — the pawn stops being a spreadsheet** (user, 2026-08-03: "pawns will have a set of needs like rimworld, and they will display a set of moodlets like sims4… like prison architect we wont display [the need] directly… this extra layer of abstraction will help make the game feel more alive and less like a spread sheet manager"). First need: THIRST, on the wolf. The model: a need is a 0..1 SATISFACTION that depletes toward zero ([F1](2026-08-03-needs-moodlets/forks.md#f1) resolves the user's open increase-vs-decrease question — one dialect for every future need; the player never sees the scalar, so its direction is pure implementation), stored as `(satisfaction, set_tic)` on the PAWN SHARD ([F6](2026-08-03-needs-moodlets/forks.md#f6)) and NEVER ticked — value and band-crossing tics are computed from the DSL `deplete` rate ([F4](2026-08-03-needs-moodlets/forks.md#f4), the TICS/TILE posture). Conditional moodlets (Thirsty, Dehydrated) are DERIVED bands — zero events while a need drains, no grant/revoke races ([F2](2026-08-03-needs-moodlets/forks.md#f2)); timed moodlets get a stored lane built now, exercised by the ACTION stream (drink on a water tile — the declared successor). ONE eval implementation in shared rust, wasm to the client, crate import to npc ([F3](2026-08-03-needs-moodlets/forks.md#f3) — a TS mirror is the two-copies drift class of subframe-ingest I8 / lod-aftermath I3). Mood = one clamped scalar, Sims emotion categories deferred ([F5](2026-08-03-needs-moodlets/forks.md#f5)). Details panel shows MOODLETS ONLY — no need bars, ever. Exit = a wolf living full→Dehydrated on a drill-scaled rate, panel + npc flipping at the SAME computed crossing tics, user's eyes close. |
| [2026-08-02-render-performance](2026-08-02-render-performance/README.md) | open | **Stop blocking the render on art** (user, 2026-08-02: "there is a reason that was async and making it sync breaks us. We will load using flat geometry until we get the real texture"). `TextureResolver.ensureCoPack` gates a stem's entire reveal on ALL FOUR maps — four fetches plus four `createImageBitmap` decodes must finish before one pixel draws, so `layers` (the least visually important) holds `albedo` hostage. The renderer already has the right answer in the GEO tier (flat tinted geometry, no network, no atlas) and `Viewport.channels` falls back to it unconditionally — the barrier is what turns a progressive upgrade into a wait. P0 instruments, P1 removes the await, P2 moves decode off the critical path, P3 adds a placeholder tier that is explicitly not a ladder, P4 the standing costs. Performance only: what is drawn once art lands does not change. |
| [2026-08-06-lora-style-not-species](2026-08-06-lora-style-not-species/README.md) | open | **Teach the LoRA a STYLE, not a bestiary** — successor to [lora-beat-e07](2026-08-02-lora-beat-e07/README.md) (~20 GPU-h, 10 runs). **The measurement it rests on ([I1](2026-08-06-lora-style-not-species/issues.md#i1)): 200 species / 701 images, and 176 of 200 have EXACTLY 3 images — one per direction.** So the corpus holds 701 examples of the RimWorld convention and ~1 example of each species, and ten runs trained one model to learn both. That explains everything unexplained there: cat (3 images) and bear never moved at any LR/alpha/corpus/warm-start, wolf-east was always strongest because the whole canine group teaches one body plan, and the convention itself was always learned early because it is the only thing with 701 examples. **Thesis ([F1](2026-08-06-lora-style-not-species/forks.md#f1)): caption for convention + body plan + direction ONLY, drop species tokens — the LoRA gets 701 examples of one concept and the BASE supplies "what a tiger looks like" from internet-scale training.** Config frozen at the predecessor's settled values (animagine, full corpus, 768, dim64/alpha64, LR 5e-5, single stage, stop at 12); the caption is the only variable. Carries forward that hue is re-tinted downstream, ControlNet constrains outline not interior, and two identical runs differ by 17.31/255 so only STRUCTURAL comparison is readable. The data ceiling is named, not fixed: specific animals need ~10-20 images each, which is a different project. |
| [2026-08-02-lora-beat-e07](2026-08-02-lora-beat-e07/README.md) | open | **Build the best quadruped LoRA we can, fresh — non-photoreal base + the 3090's bf16/24 GB, user picks the epoch** (user, 2026-08-02: "write this up with the intention of **starting new** … we are **not intending to create a valid comparison**, we are intending to create the **best lora we can** utilizing lessons learned … and advantages of the new hardware"). **Headline lesson ([I5](2026-08-02-lora-beat-e07/issues.md#i5)): every run to date, `e07` included, was trained onto `cyberrealisticXL_v80` — a PHOTOREALISM finetune — because before 2026-07-28 the box held no other SDXL checkpoint. The target art is flat regions bounded by hard outlines, so the LoRA was spending capacity fighting its own base, and that fits run-4/5's framed-portrait south and sitting-pose east.** Base moves to Illustrious-XL v1.0 ([F4](2026-08-02-lora-beat-e07/forks.md#f4), reasoning not measurement — P1 settles it with a no-LoRA probe across Illustrious/Animagine/cyberrealistic). Attribution is EXPLICITLY not a goal ([F2](2026-08-02-lora-beat-e07/forks.md#f2)) — many variables move at once and the accepted cost, recorded so nobody re-litigates it, is that a worse result won't tell us which change did it. Carried forward: `iou_ref` agrees with the eye and survives a base change (it scores against the real corpus sprite), ESRGAN outlines 43.1→90.1, restore v1-range fill sd **0.148** (run-5's jitter only reached 0.027 — [I1](2026-08-02-lora-beat-e07/issues.md#i1)), config re-chosen on 24 GB not the old 11 GB ceiling ([I4](2026-08-02-lora-beat-e07/issues.md#i4)). `e07` stays the BAR (26/36, `iou_ref` 0.753), not a control. |
| [2026-08-01-tree-occupancy](2026-08-01-tree-occupancy/README.md) | open | **2×3 trees + tile occupancy** (user, 2026-08-01: trees become 2-wide × 3-tall primitives PHYSICALLY occupying their bottom 2 tiles; biome generation must space them; "we will develop a method to handle tile occupancy so that we can improve our pathfinding next turn" — pathfinding itself is the DECLARED SUCCESSOR, this stream builds the query it consumes). Masters stay SQUARE pow2 (F1 — `size 3` drawn box, subject letterboxed 2×3, the bbox/subframe chain recovers true proportions; aspect plumbing stays dead); footprint (`&thing.footprint.w/h`, base-row anchored) is DATA-corpus, not presentation; spacing is a PURE PER-TILE TOURNAMENT (F2 — candidate iff roll < p, place iff it beats every candidate in the footprint-conflict window, each neighbour's roll recomputed from its own coordinates via a new `^rand.at` — order-free, cross-zone-correct, no stored state); occupancy is a DERIVED per-zone worker bitset (F3 — things × footprints stay the only truth, rebuilt on load, maintained on mutations, exposed as `occupied(zone, tile)` + a debug dump, consumed by NOTHING until the pathfinding stream). Exit = the user's eyes on a regenerated forest at both zooms. |
| [2026-08-01-speculative-direction](2026-08-01-speculative-direction/README.md) | done | **Facing follows the motion, not the server** (user, 2026-08-01: "when pawns are moving they will ignore the direction provided by the server, instead they will use the direction they are actually moving" — kills the pop where the server updates rotation late while the pawn is already gliding). The model: TWO sources, motion outranks the wire — in motion, facing derives from the RENDERED per-tick delta of the chase position (never `obj.facing`; a mid-motion row steers position but cannot turn the sprite), with a dead-zone + dominant-axis + minimum-hold hysteresis so e/w-first greedy stair-steps cannot flicker; at rest, the server is authoritative and ADOPTED — but only from rows that arrive while resting (the landing row's facing is stale by construction, F1), which also closes today's inverse gap (an existing mover's row never reassigns facing, so a standing pawn turned server-side stays stale). `walkGreedy`'s spec-space facing demotes to the first-frame aim (F2 — reseeds and the chase's per-axis path make spec-space disagree with what is rendered). The records inherit for free: the prim rotation lane + `cast_type` ns↔ew selection follow whatever facing the mover reports. |
| [2026-08-02-render-performance](2026-08-02-render-performance/README.md) | open | **Stop blocking a draw on a fetch.** (user, 2026-08-02: *"the first thing we are going to do is remove the await for all of the textures to load, there is a reason that was async and making it sync breaks us. We will load using flat geometry until we get the real texture"*). `ensureCoPack` awaits ALL FOUR maps — four fetches plus four `createImageBitmap` decodes — before a stem draws a single pixel, so `layers` (the least visually important) can hold `albedo` hostage. The renderer already has the right resting state: the GEO tier draws flat tinted geometry with no network at all, and `Viewport.channels` falls back to it unconditionally — so geo is the RESTING STATE and art is an async upgrade into it ([F1](2026-08-02-render-performance/forks.md#f1)), tested by denying the network entirely. Carries [I2](2026-08-02-render-performance/issues.md#i2) (a failed pack never calls `emit()`, so a stem whose bytes arrived can sit on geo for the whole session with no re-bake scheduled) and [I3](2026-08-02-render-performance/issues.md#i3) (`node_visual` re-runs the DSL VM hook per TILE in `zone_tile_prims` — a per-kind value recomputed per tile, which froze the client outright when six lookups were added to it). A placeholder tier is explicitly NOT the deleted LOD ladder ([F2](2026-08-02-render-performance/forks.md#f2)): never chosen by zoom, never feeding geometry, so [subframe-ingest I7](2026-08-02-subframe-ingest/issues.md#i7)'s bbox instability cannot return. Records [I1](2026-08-02-render-performance/issues.md#i1) as a standing warning: a 3.2 ms frame-time sweep driven by `__zoom()` in a near-empty scene was read as "no performance problem" — frame time is silent about load latency, decode stalls, GC pressure and memory ceilings, which is what was reported. Exit = first-paint and populated-frame numbers moved against a P0 baseline. |
| [2026-08-02-lod-aftermath](2026-08-02-lod-aftermath/README.md) | open | **Fix forward from the LOD rework — don't roll back** (user, 2026-08-02, after one-resolution's P6 diagnosis; F1 records the evidence: the pre-stream A/B FROZE the renderer under tick storms HEAD survives — the invalidateAll stampede I11 fixed — and rollback would also resurrect duplicate tiers and discard the restored preview kick). The bug ledger, each investigation→fix: **P0 black flora** (colour IS the layers reconstruction; the layers quadrant is EMPTY on both twins while disk bytes are healthy — the independent-per-map pack landed incomplete; COORDINATE with the armed render-performance session first, its I2 seam), plus a regression net counting packed stems with empty listed-map quadrants; **P1 the "clipped" canopies** = lighting darkness (unlit A/B proved geometry whole) — isolate shadows vs N·L vs wrap floor by A/B, fix the named term; **P2 z-order + the settled DEPTH MODEL** (F2, two user diagrams: screen.z ≡ unit.y — the base-row painter WAS the screen.z sort; the height term is the sprite-vs-ground-behind gap the zdepth lane already encodes; lands ONE shared ground-contact-row derivation for its four consumers, the back-caster shadow compare, and presence on EVERY occupied tile — ~free CPU, likely a GPU win); **P3 pan at zoom extremes** — a FOREGROUND drill (the hidden-tab rAF phantom is recorded; never re-diagnose it as a bug), then fix what it names; **P4** the full verdict on the user's eyes. Constraints: the packing LAW (F3) + twin split (F4) are settled; `__gather` occupancy joins every acceptance (an all-zero shadow buffer hides from `__lightexact`). |
| [2026-08-02-one-resolution](2026-08-02-one-resolution/README.md) | done | **Delete the atlas LOD ladder — hold ONE resolution per stem and resize in the shaders** (user: the old-game theory was *"don't ship every texture at full size"*, but this game is heavily zoom-reliant and always passes the maximum texture size, so the ladder is complexity that no longer earns its keep). Mipmaps + the recently-restored AA do the minification the ladder faked. **TWO unrelated things are called `lod` and only one dies** ([F1](2026-08-02-one-resolution/forks.md#f1)): the ATLAS LADDER goes — `LOD_SIZES`, `targetPx`/`setTargetLod`, per-(stem, size) co-packs and IndexedDB rows, `pickLodForSize`, the `FLOOR_LOD` preview tier, upgrade kicks re-emitting `onLoad`, `LodPool`'s per-size duplication, and the def-block swap that made *"the bound atlas page switches with zoom"* a standing gotcha (the `atlas-size-grows-gotcha` memory); the SLOT-GRID level stays and is RENAMED to **partition level**, because `SquareCache` already speaks "partition/re-partition" for exactly that operation — it is load-bearing (the display torus, `TEXTILE_LIGHT >> level`, `TEXTILE_UNIT >> level` for the shadow buffer, every bake's scratch) and only its name was ever the problem. The maximum size comes from the MANIFEST, not a constant ([F2](2026-08-02-one-resolution/forks.md#f2)), so an art-resolution bump stays a content change — the same posture as the SEED ppu lane. Simplifications: `packed` collapses `Map<stem, Map<size, frame>>` → `Map<stem, frame>`, ONE def block per stem so the CPU never swaps def ids with zoom, and the atlas page registry stops moving under the lighting pass when the wheel turns. **What must not regress:** the bake samples at `slotPx` texels/tile, so zoom 0.25 is a 4x minification of the master — exactly the case the ladder existed for. Acceptance is A/B captures at the deepest zoom against pre-stream ones: equal or better, or the mip sampling is wrong. Memory is the other watch. **Rides along: the PACKING LAW** ([F3](2026-08-02-one-resolution/forks.md#f3) — a concurrent stream apparently broke away from pow2 and the pool only WARNS, which is how it slipped; user: "there are pros and cons to both. But we do need to remain consistent"): P0 audits the live atlas for violators, F3 records the pros/cons and chooses SQUARE POW2 (every addressing invariant — def grid, co-pack quadrants, ppu lane, co-registered mip chains — assumes it; ES 3.0 NPOT only buys margin bytes), the warn becomes a REJECT at ingest, and violators re-ingest BEFORE the one-resolution rebuild lands on top. |
| [2026-08-01-z-positioning](2026-08-01-z-positioning/README.md) | delivered | **Height as the one truth — align the pawn's head and body shadows by giving the head an ELEVATION instead of a y-fudge** (user, 2026-08-01: *"if we instead used the same x/y as the body, but increased z, could we offset the head in y based on z, thereby aligning the body and head shadows?"*). Yes, and it is the more correct model: a y-offset and a z-height look identical on screen and mean **opposite things** to the shadow system. Today the head sits at a different world position, so it casts from a different ground footprint — two shadows, offset by exactly the fudge that made the drawing look right. Give it the same position and a height, and the y-shift becomes a **rendering consequence** rather than an authored lie; the shadows align **by construction**. Checked before planning: the `u8 unit.z` lane EXISTS and nothing else does — no DSL height, no `MoverPart.offsetZ`, no z→y projection, and **no call site writes `unitZ` at all**. That last one is load-bearing: `Lz` is 0 for every light, so the caster height test `Lz·(1−t) ≤ H` collapses to `0 ≤ H` and **is not discriminating at all** ([I1](2026-08-01-z-positioning/issues.md#i1)) — very likely feeding the "shadows aren't anchored at the base" defect. Phased so the geometry is true before the head moves onto it: P0 gives lights a height so elevation is measurable at all, P1 makes the caster card span `[z, z+H]` instead of `[0, H]` ([F3](2026-08-01-z-positioning/forks.md#f3) — the half that makes it *correct* rather than merely looking right), P2 writes ONE `elevationOffset()` in TS **and** GLSL off the live `worldTiltDeg` ([F2](2026-08-01-z-positioning/forks.md#f2), the `lightReach` pattern that has already paid for itself), P3 moves the head onto it, P4 settles where a thing *stands* for lighting too — folding in [lighting-rework I13](2026-07-31-lighting-rework/issues.md#i13) defect 4, since "drawn position vs ground position" is the same question. Names the new quantity **`elevation`**, because `z` already means draw order in four places here ([I2](2026-08-01-z-positioning/issues.md#i2)). Generalises free to wall torches, carried items and anything off the ground. |
| [2026-08-02-subframe-ingest](2026-08-02-subframe-ingest/README.md) | open | **Crop the art ONCE, at atlas ingest, from a DSL-authored subframe** (user, 2026-08-02: *"we are going to handle this on ingest … copying the subframe into the atlas instead, using the same subframe and offsets for each map. Using the anchor to align the ingested subframe into our atlas"*; and *"our dsl will define subframes for each direction individually as each direction may have different specifications as they are different textures"*). The subframe was never wrong as a CONCEPT — it was wrong as a SECOND COPY: [normal-frames I8](2026-08-02-normal-frames/issues.md#i8) had the def carry the art's bbox quantised to whole units while the anchor it was re-seated against was the CONTINUOUS opaque bottom, two derivations of one edge that drifted half a unit and painted a halo around every sprite. The 2026-08-02 repair deleted the subframe and put placement + sampling on the frame, which closed the drift and paid for it by re-opening [lighting-visual I1](2026-07-31-lighting-visual/issues.md) (the plan line back on the letterbox margin, not the feet). **Cropping at ingest leaves the duplication nowhere to live**: the atlas holds only art, the frame IS the art, and the anchor is AUTHORED rather than recovered from pixels — placement, sampling and all four maps registered by construction. The seam already exists: `packCoPack` applies ONE `AtlasDraw` to all four sources, and `sprite_scale`/`sprite_anchor` are the working precedent for a DSL layout number applied at exactly this stage. Subframes are FRACTIONS ([F1](2026-08-02-subframe-ingest/forks.md#f1) — a rounded rect is what drifted, and fractions are lod-independent), scaled to FIT the quadrant with aspect preserved so the frame stays square pow2 ([F2](2026-08-02-subframe-ingest/forks.md#f2) — `ppu` depends on it, [I3](2026-08-02-subframe-ingest/issues.md#i3)), per direction but with west DERIVED by mirroring east ([F4](2026-08-02-subframe-ingest/forks.md#f4) — west is not a different texture, and two numbers for one image is the failure being removed). The anchor takes the def's GREEN word, left free for exactly this when the subframe was deleted ([F3](2026-08-02-subframe-ingest/forks.md#f3)), which is what closes lighting-visual I1. Masters on disk stay square pow2 — this is a GPU-side ingest step, not a pipeline change ([I4](2026-08-02-subframe-ingest/issues.md#i4)). Exit = zero fringe rows on the `100,48` bush and the plan line on the feet, both measured against a P0 baseline. |
| [2026-08-02-normal-frames](2026-08-02-normal-frames/README.md) | open | **Normal maps are authored in CAMERA space and consumed as if they were WORLD space** (user, 2026-08-02: *"normals need to be pitched down 90−world_angle so they are parallel with the ground"*). Verified before planning: `design/de-lighting.md` fixes the format as *"a flat opaque background (#8080FF outside the silhouette)"* — `(0,0,1)`, out of the IMAGE — and laigter/Marigold both work from the drawn image, which the design doc already calls *"camera-distance"*. `sceneNormalAt` returns `normalize(n*2-1)` and stops; `ldir` is world-space since [z-positioning P2](2026-08-01-z-positioning/completed.md). A dot product across two frames is not a shading term, so every surface is shaded as though the camera looked straight down its own normal — partly masked by the wrap floor `mix(0.25, 1.0, ndotl)`, which is why the fix will move apparent BRIGHTNESS and not only direction. **ONE rotation, on billboards only** ([F1](2026-08-02-normal-frames/forks.md#f1)): `design/art-style.md`'s most-emphatic section says *"the world grid is viewed top-down, but objects and characters are drawn in an oblique three-quarter top-down perspective"* — **two projections by design**, so the ground's flat `(0,0,1)` is already straight up and must NOT be touched, while a sprite's is tilted up from its card's horizontal by the art's obliquity and pitches down by it. That is the user's formula as written, and `90 − 65 = 25°` reads as sprites drawn 25° above eye level, matching *"front or sides, NOT a true bird's-eye"*. (My first framing assumed one camera and invented a second, ground-side rotation; it would have broken a working case.) What remains is [I5](2026-08-02-normal-frames/issues.md#i5): `WORLD_TILT_DEG` is renderer geometry and the obliquity is a property of how the art was DRAWN — tying them is a convention, not a derivation, and if the corpus is regenerated at a different obliquity the constant must split. Rotates the NORMAL rather than the light ([F2](2026-08-02-normal-frames/forks.md#f2)), since the light is already world-space, which also collapses the two `ldir` frames into one. Carries [I2](2026-08-02-normal-frames/issues.md#i2) (west-facing art is the east master MIRRORED, and a mirrored normal map needs its X negated — unverified) and bounds [I3](2026-08-02-normal-frames/issues.md#i3) (the art is oblique, so a card is an approximation; authoring against the card plane is a content change, out of scope). |
| [2026-07-31-lighting-performance](2026-07-31-lighting-performance/README.md) | open | **Steady-state gating — stop re-lighting an unchanged world** (the declared successor to lighting-visual, opened at its close with its numbers as baseline: N=16 reach-16 lights cost 9.76 ms moving and **9.55 ms static** — static ≈ moving IS the finding, nothing is gated; the unlit floor is 0.104 ms). The design already stores everything gating needs (per-light slots, stored caster identity, bit-exact withdraw/write/deposit deltas — `__lightexact` proven); this stream wires the "don't do it again" half, in build order: skip-when-unchanged (no signals changed → no passes issued), per-light scissored delta updates (one changed light re-slots ONE rectangle), dirty-rect uploads, pan increments (re-light only the entering strip). Acceptance is numbers beside the baseline + `__lightexact` bit-identity after every phase — gating may change WHETHER work runs, never what is drawn. Idle target: static 16 lights within ~2× of the unlit floor. |
| [2026-07-31-lighting-visual](2026-07-31-lighting-visual/README.md) | open | **Grounded shadows + real normal maps — the world must LOOK right before performance work** (user, 2026-07-31: normals properly implemented; shadows begin at the BASE of prims via the minimum bbox bottom-aligned to the anchor; performance only after visual sign-off). S1: the height window becomes `[0, subH]` from the anchor and the record's `C` becomes the DRAWN art's opaque bottom — `subY` stays an atlas address only (where art sits in the frame ≠ where it stands in the world; conflating them detached shadows from feet). S2: the slot pass samples the BAKED `normal-cold`/`normal-warm` composites (warm-over-cold, the blit's rule) — tiles, analytic wall normals, material detail and mover facings all inherit in one mapping; the def-quadrant sampler retires. P0 lands the in-tree fixes (intensity decode ×4 — authored 1.0 displayed 0.25; torch reach 16) + pins the per-kind base offset before touching it. Exit criterion = THE USER'S EYES at a cold load, both zooms; the steady-state/gating perf stream opens as the declared successor with this stream's numbers as baseline. |
| [2026-07-31-lighting-correctness](2026-07-31-lighting-correctness/README.md) | done | **Pick up the lighting rework and finish it PROPERLY** (user, 2026-07-31: "another chat failed to re-implement lighting"). The rework's machinery is real (flat `u16` prims, stored caster identity, per-light slots + summed map, 16 lights in 2.31 ms) but its own capability table scores 6/10 and the user's five named gaps map cleanly onto the code: **reach is derived, not stored** (fixed by user directive: `prim_data.B` splits `u10 intensity` → `u4 reach` bias+1 = 1..16 tiles + `u6 intensity`, F1 — 16 is the measured cost-dial ceiling, not an accident); **bbox unverified** against scaled/flipped/rotated art (P0 builds the def→(stored, actual) truth table, P2 measures subframes from surface presence at ingest); **silhouettes** landed in the last commit unverified — proven against a brute-force reference, plus the two unimplemented halves: `cast_type 2` n/s cards via `base + rotation` and shadows ONTO billboards from the stored `(caster, receiver)` pairs; **z-ordering** gets ONE contract (layer lane = draw zIndex = presence sort = refine pick) proven by a lit-surface-vs-drawn-surface pixel probe; **normal maps** re-implemented as per-light N·L at slot-write time (the FINE-lightmap model — the summed map inherits shading, display stays one fetch, F2) + ambient×AO from `surface.G`. Ends with the WRITTEN VERDICT (F3): does new beat old and why — fresh moving-light measurements + the structural argument (stored identity deletes the 9.29-of-10.88 ms re-search; slots make change cost ∝ changed-region × light; flat refs end the 128-bit wall; fixed-grid passes scale 16× lights → 2.5× cost), never the stale ~4×-low old absolutes (rework I10). |
| [2026-07-31-lighting-rework](2026-07-31-lighting-rework/README.md) | open | **Build the new shader-data + lighting method** (user design, `docs/intent/2026-07-31-rework.md`, which OWNS the record layouts). Everything is a **prim** — one flat `u16` index space, no set nibble, no billboard/light/tile taxonomy; three type lanes (`cast_type`/`receive_type`/`emit_type`) decide what a prim does. `definition_data` allocates **16 px per definition indexed by rotation**, so `base + rotation` subsumes the n/s caster card, the e/w mirror and the 16-cell autotile table — three special cases collapsing into one add. Per tile: `light` (8 nearest emitters) + `presence` (8 receivers, **layer-sorted**, slot 0 = the tile). Per **unit**: `shadow`, 3 px, holding WHICH caster occludes each light plus 8 `(caster, receiver)` pairs. Then per **pixel**, the stored identity places the caster's actual texture — silhouette-exact edges with no search. Dissolves all three failures that killed the old system BY CONSTRUCTION: a reference is exactly `u16` so 8 fit a texel ([strip I1](2026-07-31-lighting-strip/issues.md#i1)); the stored value IS the caster identity, so `max`-discards-which is gone ([I2](2026-07-31-lighting-strip/issues.md#i2)); and one slot per light makes every change *(light slot) × (dirty region)*, so the **entire hot/cold axis, the prev-buffers and the class-aware dirty machinery stop existing** ([I5](2026-07-31-lighting-strip/issues.md#i5)). Adjustments made for performance and errors: slots for writing + **one summed map for reading** so the display does 1 fetch not 8 ([F1](2026-07-31-lighting-rework/forks.md#f1)); `RGB10_A2` at ¼ scale, preserving today's 4× overbright, blendable in core WebGL2 ([F2](2026-07-31-lighting-rework/forks.md#f2)); **one draw for all 8 lights**, index from fragment x, **never MRT** ([F3](2026-07-31-lighting-rework/forks.md#f3)); ping-ponged per-unit shadow ([F4](2026-07-31-lighting-rework/forks.md#f4)); gated per-pixel refine ([F5](2026-07-31-lighting-rework/forks.md#f5)); reach derived from intensity by ONE shared function ([F6](2026-07-31-lighting-rework/forks.md#f6)); validate-on-write ([F7](2026-07-31-lighting-rework/forks.md#f7)); recycled indices re-checked ([F9](2026-07-31-lighting-rework/forks.md#f9)); and **every cap counts what it drops** ([F10](2026-07-31-lighting-rework/forks.md#f10)). Three design bugs found in review are carried as acceptance criteria, not notes: the `l > 4` slot split drops light 4 ([I1](2026-07-31-lighting-rework/issues.md#i1)), receiver selection breaks before testing coverage and so resolves a pixel to different surfaces per light ([I2](2026-07-31-lighting-rework/issues.md#i2)), and the span/subframe lanes are biased ([I3](2026-07-31-lighting-rework/issues.md#i3)). Phased so **P3 prices the per-pixel pass BEFORE P4 builds shadows on it** — the opposite of the old stream, which built the refine first and found it cost 9.29 ms of a 10.88 ms pass. Target: moving lights at reach 16 inside 8 ms, from the stripped floor of 0.045 ms/frame. |
| [2026-07-31-lighting-strip](2026-07-31-lighting-strip/README.md) | done | **Completely strip the lighting + shadow system, so it can be re-thought** (user, 2026-07-31). Removes ~4 300 lines (`shadowGather.ts` whole, the lighting half of `coldShadowData.ts`), **~83 MiB of render targets** (4 shadow + decay + 2 receiver + 2 lightmaps at 32 MiB each), **6 of the frame's 8 steady-state draws**, three data-texture bands (`light_data`, both `light_presence`) and four design/intent docs. NOT a rewrite: it deliberately stops at a renderer drawing unlit albedo, because the replacement model is the user's to author ([B1](2026-07-31-lighting-strip/blockers.md) states the question and gates nothing). The case for stripping rather than patching is structural and recorded with evidence: a shadow texel is **128 bits and every extension needs more** — a caster reference is `u20`, 8 of them is 160, and MRT hung Chrome twice while a 2×-wide RT runs the 87 %-of-frame walk twice ([I1](2026-07-31-lighting-strip/issues.md#i1)); `max` keeps HOW MUCH and discards WHICH, which cost a **9.29 ms refine of a 10.88 ms pass** before deletion ([I2](2026-07-31-lighting-strip/issues.md#i2)); penumbra was computed and thrown away for an unknown number of streams (**31 155 shadowed samples, zero partial**, [I3](2026-07-31-lighting-strip/issues.md#i3)); z-order resolves **per pixel** in the hottest shader ([I4](2026-07-31-lighting-strip/issues.md#i4)); and the differential was designed, allocated and **never wired** — 4 MiB idle since creation ([I5](2026-07-31-lighting-strip/issues.md#i5)). Surgical, not scorched-earth ([F5](2026-07-31-lighting-strip/forks.md#f5)): the primitive graph, the G-buffer, the art pipeline's normal/depth maps and F8 self-positioning all outlive the system that motivated them. Phased so it is reversible — **P0 records everything before deletion** (reference images, per-pass ms, resident bytes, a capability checklist), P1 cuts consumers with the code still compiling ([F3](2026-07-31-lighting-strip/forks.md#f3)), P2 deletes, P3 retires the bands, P4 archives the docs and closes eight superseded streams, P5 leaves a documented seam with the measured ms budget the replacement inherits. |
| [2026-07-30-pawn-part-placement](2026-07-30-pawn-part-placement/README.md) | closed | **SUPERSEDED 2026-08-06** by [human-pawns-redux](2026-08-06-human-pawns-redux/README.md) F5 — its plan was written against `content/visual/pawns.rd` variables DELETED with the DSL (`head.scale 0.625`, `head.offset.y −1.15`; the corpus now authors `[[thing.part]]` TOML: `scale 0.5`, `offset.z 0.87`). The facing z-order it wanted was later built (per-slot `depth` + `facingDepth` — pawn-render); placement tuning is plain TOML judged in the browser (redux I3); the lighting-footprint + capture lanes carry as redux [I9](2026-08-06-human-pawns-redux/issues.md#i9). Superseded, not delivered — kept for the I1 blit-footprint finding (a standing billboard is lit by the ground it is DRAWN over, up to a tile off) which remains real and unbuilt. |
| [2026-07-30-human-pawns](2026-07-30-human-pawns/README.md) | done | **Extend the pawn class: multi-part pawns, persistent variants, the first human** (user, 2026-07-30). A pawn stays ONE object (one entity, one DSL kind) but generalizes to a PARTS list declared in its visual DSL: the wolf = 1 part, a human = body (part 0) + head (part 1, `scale 0.625`), hands eventually — no bespoke classes. Content spec: per sex 9 body variants (fit 0-2, fat 3-5, average 6-8) + 16 heads applicable to any body; art already mastered at `textures/pawn/human/{female,male}/<variant>/<map>.<dir>.<part>.png`. Per-pawn state generalizes NOW (user F1): pawn defs repack to real `TYPE_PAWN\|species\|kind\|variant` defs (the wolf's variant = `def.variant_id`; today it is id-derived + only canonical art is served) and pawns gain a growable `payload: Vec<u32>` opcode stream (command-buffer style: `opcode:16\|count:16` + operands) — first opcode `PART(slot, def)`, the FULL def a part slot draws, so armor later swaps a slot by rewriting one entry, and future inventory/stats/needs are just more opcodes. The payload lives in SIDECAR tables `payload_log`+`payload` SLAVED to state (user F5: never claimed independently — the entity's state claim IS its lock, writes ride state-write transactions), so movement hops never copy it (`data:u8` facing/serial unchanged). Delivered at mint by variable-arity `CREATE def·position·count·payload` (F2). Staged: P0 packed defs + payload + CREATE + wire fan (live subscription check) → P1 variant+part stems served (`<base>/<variant>/<facing>.<part>`, F3) → P2 loader reads ALL `prims.N` + the human corpus (`content/visual/pawns.rd`) → P3 MoverLayer renders parts uniformly (wolf = degenerate 1) → P4 the npc spawns ONE STATIC human at a known location — doubling as the standing static-pawn drill fixture — user's eyes → P5 graph conformance: head attaches as a carried piece of the body's carrier per VARIABLES.md ("a pawn = prim{head, body, …}"). _(Since delivery: the visual corpus moved to `[[thing.part]]` TOML with the DSL's deletion, the fixture's console mover died with input-rework, and the mint path is re-opened by [human-pawns-redux](2026-08-06-human-pawns-redux/README.md).)_ |
| [2026-07-29-analytic-wall-normals](2026-07-29-analytic-wall-normals/README.md) | open | **Exact normals for regular walls; the exact one seeds the rest** (user, 2026-07-29 — "smooth with predictable geometry, so we should be able to handle this set mathematically… once we have the first correct normal… use that to generate a control net for others"). ASSESSMENT: the day's numbers argue FOR it — learned inference moved relief ±4 pts on identical input, collapses off its trained 768, and every consistency defect existed because a model guessed at a beveled PRISM whose true normal map is computable. Model: `H(x,y) = profile(d)` — the wall's cross-section (outline→face→bevel→top, FITTED to the drawn art with a ≤1 px register check) swept over each cell's D1 connectivity shape (F1: shape from bits + params, NOT alpha tracing); normals = grad H in the corpus encoding. Consistency is ZERO BY CONSTRUCTION — no alignment, no stamping, no seeds; the n/s asymmetry of the oblique art is respected (N arms = back/top, S = front face). Staged: P0 profile fit + register → P1 the generator (`marigold/atlas_geometry.py`, pure numpy) gated by atlas_check → P2 `bin/art normal --analytic` + the in-game drill → P3 the ControlNet spike on ONE kind (brick) — `controlnet-union-promax` IS on the box; conditioning = the analytic normal (and/or its free height field as depth) + the brick diffuse; hard gate (consistency ≤2× smooth, relief ≥ baseline) + a GO/NO-GO for the all-kinds follow-on. Marigold+alignment stays the path for genuinely unpredictable geometry. |
| [2026-07-29-comfy-linked-tiling](2026-07-29-comfy-linked-tiling/README.md) | open | **Seam-inpaint the linked atlas SOURCE via ComfyUI so cells truly tile** (user, 2026-07-29 — "feed it through comfyui to get it to modify it slightly so it tiles properly"). The normal-side stream made the NORMALS consistent but compensates for per-cell variation in the DIFFUSE: albedo seams remain, and stamped normals ("identical run") can disagree with per-cell-varying texture. Stance: inpaint on assembled IN-WORLD LAYOUTS (runs/corners/Ts/cross via the D1 table — atlas order is not world adjacency, the lesson both prior streams hit), masked to ~2-unit SEAM BANDS at low denoise (~0.35; outside-mask bit-identity is an ACCEPTANCE, not an assumption), with canonical edge bands FROZEN once finalised (F1 — any E-cell must abut any W-cell, so a fixed seam must hold in every pairing). Diffuse ONLY; every derived map regenerates through the existing chain (F3 — independent inpainting would desynchronise them). Staged: P0 albedo-seam metric joins `atlas_check` + baseline → P1 deterministic layout compositor (bit-identical round-trip) behind `bin/art retile-linked --dry` (F2: `bin/lib/retile_linked.py`, reusing generate.py's ComfyUI client) → P2 the live inpaint pass, gated by the metric → P3 regenerate maps + re-gate relief/normal-seams AND weaken donor stamping if the consistent source makes it unnecessary (recovering per-cell character — I1's compromise, not a goal) → P4 the in-game torch drill, the user's eyes final. |
| [2026-07-29-marigold-linked-normals](2026-07-29-marigold-linked-normals/README.md) | open | **Consistent normal frames across a linked atlas's 16 cells** (user, 2026-07-29 — "we are very close, but due to the tiled nature we're having some issues with consistently aligning normals"). Marigold predicts view-space normals over the WHOLE atlas as one scene, so each autotile cell lands in its own frame: flat tops drift off +Z per cell, equivalent pieces get different slopes, in-world-adjacent edges disagree — now VISIBLE as per-tile shading jumps since texture-generalization P4 lit walls via `tileNormal`. Stance: KEEP Marigold, fix the frame (F1; composition-from-canonical-pieces is the fallback). Staged: P0 a measuring oracle (`marigold/atlas_check.py` — flat-frame error, same-piece deviation, D1-adjacency seam continuity; baseline recorded) → P1 per-cell inference hygiene (replicated-edge padding — atlas order is NOT world adjacency — pinned seed, ensemble) → P2 global frame alignment (flat-cluster rotation to +Z + bevel-gain match) → P3 symmetry averaging (vector-aware mirror transforms) + seam enforcement → P4 regenerate + the in-game torch drill at ≥2 zooms (the user's eyes final). Lives in `marigold/` behind the existing `bin/art normal --marigold` entry (F2). |
| [2026-07-29-linked-cell-pad](2026-07-29-linked-cell-pad/README.md) | open | **Per-cell guard padding on linked textures, authored in the corpus** (user, 2026-07-29). A linked sheet (`biome-tile/default/smooth/wall`, grid 4×4) carries a guard ring around each sub-image; the client must trim that ring off the cell's sampling rect so a cell never bleeds its neighbour. **The trim already exists** — `TextureResolver.cellFrame` insets all four sides — and so does the user's step 3: `cellFrame` returns a `TexFrame` (a rect on the page), so a smaller source rect over the same destination IS the scale-up, with no resample and no cost ([I1](2026-07-29-linked-cell-pad/issues.md)). What is missing is only the SOURCE of that inset: today it is `entry.pad` off the texture manifest, and the user wants it authored per object. So this is a plumbing stream — `content/visual/*.rd` → `VisualParts` → def payload → `cellFrame` — not a rendering one. Authored in **px per cell edge at the art's native cell size** ([F2](2026-07-29-linked-cell-pad/forks.md#f2)), the only unit countable off the source art and the only one that survives a grid change; converted `pu = (padPx / nativeCellPx) / cols`, which is LOD-invariant. Named **cell**, not slot ([F1](2026-07-29-linked-cell-pad/forks.md#f1)) — `slot` is the textile SLOT grid, the concept behind [map-compatibility](2026-07-24-map-compatibility/README.md). DSL overrides, manifest falls back ([F3](2026-07-29-linked-cell-pad/forks.md#f3)), because [art-128-tiles](2026-07-28-art-128-tiles/README.md) is concurrently making the pipeline emit a correct `padU`/`padV`: the manifest states what the art HAS, the DSL states what we want used. Open question held for the eye, not the argument: trimming 1 px off a 128 px cell leaves 126 stretching to 128 — a **non-integer magnification**, which every other map avoids by construction and which `pad_maps.py` already hit from the writer's side (1022 px over 8 cells = 127.75 px/cell). [F5](2026-07-29-linked-cell-pad/forks.md#f5) decides it from a screenshot in P3; if it bites, the principled fix is pad OUTSIDE the nominal cell, which is art-128-tiles' territory. |
| [2026-07-29-texture-generalization](2026-07-29-texture-generalization/README.md) | open | **Linked textures ride definition_data; tiles enter lighting via prim_presence slot 0** (USER-authored design, 2026-07-29 — supersedes tile-lighting's per-prim approach; that stream paused with its audit + manifest-race fix landed). The def describes the WHOLE linked atlas (frame_x/y + span = grid tiles, top-left anchor); the DSL carries the linked grid, a NEW `internal_padding` (units — walls: 14×14 window, offset 1), and `cast_shadow` (false on ALL tiles incl. walls — wall shadows later); def R bit 24 becomes cast_shadows; `rotation` becomes a PER-TYPE MODE for tiles (0 plain / 1 autotile / 2 world-hash later / 3 reserved; walls = 1); `u4 type` keys one path for all biome-tiles. `prim_presence` slot 0 = the tile's DEFINITION id directly (no prim_data per tile — frugal; ids 1-based, 0 = no tile; casters 1..7, early-out flips to slot 1). Adjacency resolves AT READ TIME from neighbors' slot-0 defs (rotation-1 match = connected; change a tile → dirty the ring → neighbors self-heal in-shader — no CPU mirror, no manifest race). Assessment: fills def fields the format RESERVED for DSL authoring, deletes the failure class just debugged, dissolves the per-cell-def conflicts (offset-lane + quadrant-stride); risks pre-registered R1–R6 (slot-0 contract/early-out, id namespaces, the ground-shadow cut must not flip, connect rule, padding vs masters, CPU-drawn cells v1). Blueprint hot lighting carried over (F3). |
| [2026-07-28-build-walls](2026-07-28-build-walls/README.md) | done | **The build panel, wall placement, and linked-tile rendering** (user, 2026-07-28). A BUILD panel (DomPanel, category sub-tabs — wall first) whose icons come from build-TAGGED kinds in the DSL (D3 — adding a brick wall in content alone lights a second icon; icon = the linked atlas's cell (0,1) via the resolver, D6); icon click → PLACEMENT MODE (right-click exits, ui-select semantics suspended, D4): left-drag forms a rect previewed CLIENT-SIDE on its perimeter with the `blueprint/wall` linked textures, variant-aware; release → **`BUILD_WALL` (action 9)**: start tile + end tile + u32 object id through core → event shard, worker builds IMMEDIATELY (perimeter tiles → the wall kind via the SET path) — with the FUTURE-INTENT note pinned: the same order later creates a visible blueprint that pawns must build, so the event shape must not bake in "immediate" (D5). Ships the linked-texture pathway's documented Phase 2: `thingTexture` already routes linked kinds at "canonical cell 0 (still Phase 2)" and the resolver already narrows manifest-gridded cells — this stream makes the cell the NEIGHBOR-CONTEXT cell. **D1, derived from the user's 16-row table: atlas x = N + 2·E, atlas y = 3 − (S + 2·W)** (all 16 rows check; table test pins it). Variant selection stays CLIENT-side, the shard stores the KIND only; a changed tile re-expands its 4-neighbor ring across row/zone seams (D2 — the seam is the acceptance). Both masters already on disk. **DELIVERED 11/11**: the drill ran the WHOLE loop live — icon (content-lit, atlas-cell CSS crop) → crosshair mode → drag → correctly-jointed translucent blueprint ring (client-only) → release → worker `build_wall expanded queued=16` → 16 per-zone-routed PROMOTE SETs → the wall ring rendered with correct joins, reload-persistent, 120.2 fps; the hand-painted L proved every neighbor cell exact (corner 15, ends 4/8, mids 6/9) across the x=96 seam. P0 content+formula → P1 linked rendering → P2 panel+mode+preview → P3 event end-to-end → P4 drill. Out: the blueprint-entity phase, demolition, occupancy, diagonal autotiling. |
| [2026-07-28-ui-select](2026-07-28-ui-select/README.md) | done | **Mouse interaction, selection, and panel layering** (user, 2026-07-28). Left click SELECTS (topmost warm mover by painter order → cold thing by tight box → the TILE — one `SelectionModel`, Set-based + change event, API shaped for the LATER drag-box pass); middle click PANS (left freed, wheel zoom unchanged); right click with a selected pawn sends a MOVE ORDER — the path already exists end-to-end (`WasmClient.moveEntity` → core `move_entity` → edge `MOVE_TO` allowlisted), so this is input wiring not protocol work. Feedback: selected objects OUTLINED (viewport overlay; silhouette preferred, tight-box fallback — in-stream fork), the CURSOR carries a small-radius HOT light through the real placement path (the FIRST live hot light — exercises the hot-light matrix cells pawn-render verified only structurally), a selected tile's world coords in the TITLE BAR. Panels: a re-implemented DETAILS panel (`DomPanel`, reads the SelectionModel + content tables), and a per-panel persisted REMAIN-ON-TOP setting in `PanelSettingsPopup` that promotes the panel's existing z BAND (`DomPanel` already bands z-indices by category — chat/options/debug float above the viewport when flagged). **DELIVERED 10/10 same day**: every acceptance measured live (wolf/tree/tile selections probed, a right-click MOVE_TO fanned through the edge and walked the wolf until the npc's supersede reclaimed it, silhouette outlines pixel-hugging at zooms 1+0.5, cursor hot light with COLD BAKES 0, 'Game View - tile x, y' in the title, details live-updating a walking pawn, chat flagged on-top surviving reload at z 50001 over the focused viewport, 120.2 fps drill). Wolf-specific visuals (sprite/outline) remain art-blocked behind ns-shadows B1. P0 input+model → P1 feedback → P2 panels → P3 interaction drill. Out: drag-box itself, ownership checks, touch, path preview. |
| [2026-07-28-hot-sync](2026-07-28-hot-sync/README.md) | done | **ONE hot dirty, so the wolf and its lighting move as one — DELIVERED 7/7 same day: 82/82 record rewrites on sprite-bake frames, residual ≤ 1 unit (max 0.067 tiles), backstop 0, cold 0, 120 fps at zooms 1 + 0.25** (user, 2026-07-28 — "our light does not stay linked 1:1 with our moving wolf… for hot we would need one [dirty] so they remain in sync"). The user's assumption CONFIRMED (the wolf rides the intended warm-G-buffer + hot-map path) and the suspicion located in code: THREE independently-gated dirty systems drive the mover's visual vs lighting — the warm sprite bake (eps 1/32-tile crossings, BUDGETED), the hot light/shadow rects (originating from `buildCasters`' record change-detection, whose UNIT-quantised position steps at 1/16-tile crossings — a structural 2:1 cadence mismatch), and the receiver-map channel — plus a frame-order skew (`moverLayer.tick()` runs AFTER `panel.tick()`, so every frame renders yesterday's mover state). Plan: P0 instruments the skew into before-numbers; P1 = the user's design ([F1](2026-07-28-hot-sync/forks.md#f1)) — one Viewport `moverDirty` call at the sprite's OWN eps crossing fans atomically into warm squares + hot cls-1 rects + receiver rects + the record rewrite ([F3](2026-07-28-hot-sync/forks.md#f3)), `buildCasters` demoted to backstop, tick order flipped ([F2](2026-07-28-hot-sync/forks.md#f2)), mover squares unbudgeted ([F4](2026-07-28-hot-sync/forks.md#f4)); P2 asserts per-frame lockstep ≤ 1 unit over a soak at 3 zooms + the 120 fps guard. COLD keeps its independent channels — that decoupling is the cold tier's cost model, per the user. |
| [2026-07-28-pawn-render](2026-07-28-pawn-render/README.md) | done | **Depth-correct wolves, lit and shadowed through the HOT maps** (user, 2026-07-28 — graphics + functionality pass on the wolf pawns). DELIVERED 15/15 same day: the blit compares the always-baked zdepth painter's keys per pixel (trees occlude wolves and vice versa, all zooms); PRIMS carry temperature (record bit 25) and movers join the hot pass as receivers + casters — with a NEGATIVE cold-light delta that carves a mover's shadow out of the cold pool against the cold shadow map, so a wandering wolf NEVER re-bakes cold (measured 0 cold bakes through every walk, 120 fps at zooms 1 and 0.25); n/s turned out to be carried by frame-follows-facing (F5 amended — only the rotation code needed wiring); en-route finds: the edge's index registration doesn't self-heal (I1, task chip) and resting pawns lost their base past half the tic ring (I2 — gc re-stamps kept bases now). Every reported symptom was located in code: the blit composites warm-over-cold by COVERAGE only while the baked `zdepth-world` front/behind key (`0x80\|baseRow`) is read by NOTHING ([F1](2026-07-28-pawn-render/forks.md#f1) — compare it wrap-aware in the blit, winner takes albedo/surface/emissive/light); the lightmap bake only ever sees COLD prims, so the wolf wears the GROUND's baked lighting — no mover N·L, receiver, or caster (P2/P3: warm billboard records by UNIFORM in the cold record format ([F2](2026-07-28-pawn-render/forks.md#f2), the hot-shadows design), movers join the HOT class pass as receivers + casters using the LIVE climb/receiver machinery ([F4](2026-07-28-pawn-render/forks.md#f4) — attempt #3's folder is stale, its receiver plan got built by later streams; P5 reconciles it)); the blit's light-select IS the user's ratified tier matrix ([F3](2026-07-28-pawn-render/forks.md#f3): warm-winning pixel = `ambient + hot` only — cold light lights the wolf via the HOT map, a moving wolf never dirties `lightmap-cold`); N/S billboards cast E/W because records hard-code the e/w regime — true rotations 0–3 + n/s frame selection through `sampleCard`/`casterCover`/`billboardNormal` ([F5](2026-07-28-pawn-render/forks.md#f5): the FRAME changes, the card plane doesn't — perpendicular-card modeling deferred), cold n/s prims fixed free. Normal maps come with P2's N·L (the user's "free once lighting works", stated as an explicit acceptance). Emissive already works (regression control). Zoom stability at ≥3 zooms is an acceptance test on every visual item (the attempt-#2 revert lesson). |
| [2026-07-28-movement-hardening](2026-07-28-movement-hardening/README.md) | done | **One chain per pawn, a 6 Hz clock, no phantom despawns** (user, 2026-07-28 — "address the issues and make general improvements based on your findings"). DELIVERED 16/16 same day (P2.5 render-chase added mid-stream by user design: the RENDERED pawn chases the speculation at ≤ +20% of true speed — spec is truth-tracking, chase is presentation). Headline discoveries while executing: `rd redeploy` never hashed `shared/codec` into module inputs so a new verb was silently unpublishable (fixed — shared crates are inputs now); an orphaned dirty claim could wedge an entity's composition FOREVER (fixed — `ABANDON_TICS` + gc reap); a shard wipe left the npc driving a GHOST (fixed — `StateGone` is trustworthy post-P2, so the brain honors removals and re-CREATEs); and the 5.4-vs-6 Hz mystery was **WSL2's `CLOCK_MONOTONIC` running at 0.900× realtime** — the master now paces a `SystemTime` grid with a half-gain integral controller, durable **5.9986 Hz** measured. Closes the three open movement/sim issues plus the session's improvement findings. P1 = **chain supersession** ([pawn-movement I7](2026-07-28-pawn-movement/issues.md)): the seed stamps a 6-bit trip-serial into the pawn's `data` low bits (facing owns the top 2 — the client's `data >> 6` never reads them, verified) and continuations become a WORKER-ONLY `MOVE_STEP obj dest serial` (action 8) that dies silently on serial mismatch — a new intent cancels the old chain BY CONSTRUCTION, making the npc's deadline re-issue safe instead of chain-duplicating ([F1](2026-07-28-movement-hardening/forks.md#f1)); the edge grows the first, deliberately tiny client-verb allowlist so `MOVE_STEP` can't be client-issued ([F2](2026-07-28-movement-hardening/forks.md#f2) — NOT an ownership model; that fork stays open). P2 = **phantom `StateGone` suppression at the edge** ([first-pawns I4](2026-07-28-first-pawns/issues.md)): entity→zone tracking per connection swallows the zone-migration delete, one-beat hold covers delete-before-insert, real removals still relay ([F3](2026-07-28-movement-hardening/forks.md#f3)). P3 = **master pacing** ([pawn-movement I5](2026-07-28-pawn-movement/issues.md), measured 5.41 Hz vs authored 6): instrument first, fix the measured stage, NO burst catch-up — a tic is a wall-time promise ([F4](2026-07-28-movement-hardening/forks.md#f4)); the learned-rate estimator stays as defense. P4 = polish/robustness: persist the learned `tics_per_sec` as a clamped boot HINT (kills the ~60 s estimator warmup, [F5](2026-07-28-movement-hardening/forks.md#f5)), migrate `gateway/directory.rs` onto `resonantdust-uplink` (the pattern's birthplace, last unconverted surface), re-verify the stale `index` module current/ doc. NON-goals held: tween-blend + re-anchor-every-N knobs, pathfinding, ownership. |
| [2026-07-28-pawn-movement](2026-07-28-pawn-movement/README.md) | done | **Content-authored speed + speculation that actually glides** (user, 2026-07-28). DELIVERED 15/15 same day. Speed is now a DSL-authored per-kind property in **tics per tile** (wolf `12 &thing.speed set` → 2 s/tile at 6 Hz) — the user's ruling supersedes first-pawns F7's wall-time authoring ([F1](2026-07-28-pawn-movement/forks.md#f1)); `codec::speed` keeps only `DEFAULT_TICS_PER_TILE` + `resolve`, the def-taking stub DELETED so the compiler found all three consumers ([F5](2026-07-28-pawn-movement/forks.md#f5)): worker (corpus from disk — [F3](2026-07-28-pawn-movement/forks.md#f3); verified hops at exact 12-tic spacing), webgl MoverLayer (new bundle `thing_speed()` table), npc deadline (corpus-resolved). The verb needed NO change — `MOVE_TO` was already destination-only, seed + landing fanned on the state channel ([F2](2026-07-28-pawn-movement/forks.md#f2)). The snap-tween-snap decomposed into four MEASURED causes, all fixed: dropped intents for unseen movers / unanchored clocks ([I1](2026-07-28-pawn-movement/issues.md) — `pendingIntents` buffer), the retention-less `event` table replaying history ([I2](2026-07-28-pawn-movement/issues.md) — `event_shard::gc` on the master's cadence, closes first-pawns I2), **the durable tic actually running at 5.41 Hz not 6** ([I5](2026-07-28-pawn-movement/issues.md), still open as a master-pacing follow-up) which the constant-rate max-anchor estimator ratcheted into an unbounded lead ([I4](2026-07-28-pawn-movement/issues.md) — measured d climbing 5.5→35 with page age, eventually letting the staleness guard eat LIVE intents), fixed by a rate-LEARNING `TicEstimate` ([F6](2026-07-28-pawn-movement/forks.md#f6): windowed re-anchor, learned `tics_per_sec` carried on `TicAnchor`, ±900-tic poison band with streak reset). Post-fix soak: **14 trips → 14 single arms, zero absent/doubled (closes first-pawns I3), arm-`d` flat at 3.8–5.0 tics, landings e ≤ 0.22 tiles** once the rate converges. Snap-first stands; tween-blend recorded as the second held knob in `ACTIONS.md` ([F4](2026-07-28-pawn-movement/forks.md#f4)). |
| [2026-07-28-square-128](2026-07-28-square-128/README.md) | done | **`SQUARE` back to 128, with the lighting dial split off and pinned at 64** (user, 2026-07-28 — triggered by a mushy wolf against crisp conifers). Shadow needs no work: `TEXTILE_UNIT = SQUARE/UNIT` with `UNIT = SQUARE/16`, so it is 16 texels/tile by construction at any `SQUARE` — units-per-tile is a WORLD invariant the wire and data records depend on. The real problem is that `TEXTILE_SQUARE = SQUARE` made **one constant steer two unrelated things**: how sharp the art is, and how many texels the lighting pass shades. That identity is why the `2b1025a` A/B ("SQUARE 128 -> 64 — lighting 3.3× cheaper, 192 MiB freed, art softer") had to pay for the lighting win with art resolution — the effects were never coupled, just co-located behind one name ([I2](2026-07-28-square-128/issues.md)). Split them via a new pinned `TEXTILE_LIGHT = 64` ([F1](2026-07-28-square-128/forks.md#f1), [F2](2026-07-28-square-128/forks.md#f2)) and the trade disappears: DELIVERED 21/21: art maps 2176×1152 → 4352×2304 (+230 MiB, measured), the three fine maps stay 2048×1024, and the lighting pass **did not move** — 0.286 → 0.290 ms at reach 8 on byte-identical dirty counts, against the 2.2–3.1× it costs when lighting follows `SQUARE` (P0 probe). `fw/fh` turned out to size THREE maps not one, so the pin is worth **216 MiB**, not the ~48 estimated ([I3](2026-07-28-square-128/issues.md)). `FINE_RATIO` stayed 4 throughout. No re-mastering was needed — 0 of 27 manifest entries were short of 128 and the wolf carries a 512 master, so `BASE_LOD_PX = SQUARE` was the whole cap ([F6](2026-07-28-square-128/forks.md#f6), art-pipeline work left to [art-128-tiles](2026-07-28-art-128-tiles/README.md)). Sequenced split-then-raise ([F3](2026-07-28-square-128/forks.md#f3)) so the split has a hard oracle (every number identical) before `SQUARE` moves, because the one hazard is a slot address: `Viewport.ts:456` feeds the lightmap `win.slotPx` (= `SQUARE >> lod`) and decoupling flips which of the two is correct — at lod 2 the two now differ 2× (32 vs 16), so that fix is load-bearing, and splitting first meant it landed while the oracle was still exact. Also a doc reckoning: `VARIABLES.md` still specifies the 128 world throughout AND contradicts itself at line 461 (`= 4px`), because the A/B landed as a `test(` commit that was never reverted or ratified ([I1](2026-07-28-square-128/issues.md)). |
| [2026-07-28-art-128-tiles](2026-07-28-art-128-tiles/README.md) | done | **The art pipeline on 128 px tiles, sized by declared tile footprint** (user, 2026-07-28). Mostly CONFORMING the tooling to a model that already exists: `thing.span` is authored in the corpus (conifer `2 &thing.span set`), `frame_span` is a ratified u4 of pow2 tiles, and the square is already `frame_span · SQUARE / 2^lod` — which at `SQUARE = 128` yields exactly the user's numbers (conifer 1×2 → 256², ground 8×8 → 1024², linked 4×4 → 512²). The hole is that `bin/art` never reads any of it: `_emit_crops` fits each blob to its own `_pow2_box`, so a conifer landing in 256² is blob extent agreeing with span by luck ([I1](2026-07-28-art-128-tiles/issues.md)). So: read the footprint, derive the square, cache `tiles [w,h]` into the leaf without minting a second authority ([F1](2026-07-28-art-128-tiles/forks.md#f1)) and without collapsing 1×2 to 2 on the way in ([F2](2026-07-28-art-128-tiles/forks.md#f2)). Also fixes `--pad`, which guards the CANVAS edge — on an atlas that leaves every interior cell boundary unguarded and pulls the sheet off its grid, while `tex_manifest.rs` already documents `pad` as a normalized PER-CELL inset the client trims, so only the writer disagrees ([F3](2026-07-28-art-128-tiles/forks.md#f3)). Depends on but does not own `SQUARE = 128` — that is [square-128](2026-07-28-square-128/README.md); this stream parameterises the tile edge so the two land in either order. **DONE 27/27.** B1 resolved by the user: linked forms convert to `sprite.l.0.png` with their per-cell folders deleted ([F4](2026-07-28-art-128-tiles/forks.md#f4)), and `sprite` is an ARCHIVE while `art normalize-square` scales every derived map to `span · TILE_PX` in both directions, warning on upscale ([F6](2026-07-28-art-128-tiles/forks.md#f6)). Final audit: 71 leaves match target, 0 undersized. Five bugs found that were not in the plan — I8 (every ground sheet was served flat, its atlas.json sharing not one key with the parser), I10 (held-whole atlases resampled to 127px/cell by the sprite-sheet path), I11 (the edge's manifest tests asserted against an empty manifest), I12, and I6 (my own fork: the square derives from `span`, not `footprint`). |
| [2026-07-28-first-pawns](2026-07-28-first-pawns/README.md) | done | **A wolf pawn in a `pawn` shard, driven by an npc container** (user, 2026-07-28). ONE wolf, server-minted, through the WHOLE pipeline: npc → client/core → edge queue → event shard → orchestrator → worker → **pawn** data shard → promote on tic → edge subscription → webgl AND npc. The pawn shard is the enabled shard-tables follow-on made real — a new `entity_tables!(data:u8)` stamp (`modules/pawn/`, auto-discovered, DB `…-pawn-0`), `TYPE_PAWN` routed to it at orchestrator claim, worker compose/write, edge relay+subscribe; `data_shard` stays the catch-all ([F1](2026-07-28-first-pawns/forks.md#f1)). P1 builds **`CREATE`** per ACTIONS.md (idempotent `spawn` reducer + spawn-log, [F4](2026-07-28-first-pawns/forks.md#f4)) — killing the client-minted id collision (`wolf_key` vs `selfEntity`) and finally plumbing `definition_reference` so wolves stop rendering with the TREE def. Movement is the user's authoritative-state model made real ([F2](2026-07-28-first-pawns/forks.md#f2), user-directed): the npc asks A→B, the WORKER chains the hops (`ACTIONS.md` §Movement UN-TABLED — self-queued continuations at `tic + tics_per_tile`, queue-at-future-tic `≥ master+3`, greedy-line step behind an explicit pathfinding seam), the INTENT fans once via `PROMOTE_EVENT`, bare continuations fan NOTHING, and clients SPECULATE position on a wall↔tic estimate — the first time clients care about tic — corrected by authoritative `state` at seed/final + whenever another event resolves the position. Speed = one `tics_per_tile(def)` seam in codec shared by worker AND client ([F7](2026-07-28-first-pawns/forks.md#f7)); corrections snap + log their error so the re-anchor-every-N knob tunes on data ([F8](2026-07-28-first-pawns/forks.md#f8)). npc splits into a `Bot`-harness lib + `Brain` trait (wolves-brain first; villager-/region-steward-brains are future impls of the same seam) and runs as a supervised container `rd-npc-<brain>` ([F6](2026-07-28-first-pawns/forks.md#f6)). Minted-id adoption from the zone's state fan-out ([F3](2026-07-28-first-pawns/forks.md#f3)); no ownership model yet (accepted). Sibling: [sim-self-heal](2026-07-27-sim-self-heal/README.md) hardens the same processes. |
| [2026-07-27-material-system](2026-07-27-material-system/README.md) | done | **Per-layer materials: colour variation + normal detail** (user, 2026-07-27). A material binds per LAYER channel (the `layers` map's RGB, `PACKED_CHANNELS` 3) and carries TWO effects: OKLab hue/chroma jitter on the albedo (machinery EXISTS but is INERT — webgl's `noiseAtlas.ts` is a null stub, so every swing samples the 0.5 fallback; P0 ports the 104-line pixijs generator) and NEW normal DETAIL (a tiling field RNM-blended onto the base normal, amplitude × the channel's layer weight — the fix for Laigter-generated normals reading plastic/flat, [F2](2026-07-27-material-system/forks.md#f2)). Solid white/identity is the default and the contract: no material ⇒ bit-identical bake (the delta form already guarantees colour; amp 0 must guarantee normals). Per-object variance via a `u8 seed` lane in `billboard_data` (B's reserved bits, [F3](2026-07-27-material-system/forks.md#f3) — VARIABLES first), stamped from the existing deterministic `cellSeed` so variation never swims. WHERE colour goes is the user's open question, held as [F1](2026-07-27-material-system/forks.md#f1) with four modes behind a live switch decided BY EYE (lean: detail-field-keyed — colour and relief sharing one cause makes clumps read). First application: conifer — `needle` field appended to the atlas ([F4](2026-07-27-material-system/forks.md#f4)), needles on layer 0 (R, foliage), mild bark on trunk. Everything lands at BAKE time under the existing budget — zero per-frame display cost. Blend-alpha rule from lighting-feel F3 honoured (attachment alpha is a blend factor). |
| [2026-07-27-lighting-feel](2026-07-27-lighting-feel/README.md) | done | **Colour, glints, and the DECAY LIGHTMAP** (user, 2026-07-27 — ratified item-by-item). The lighting is architecturally sound (per-light masks give correct multi-light cross-fill; perf handled by standing-costs) but the world doesn't FEEL lit. Build: **colour temperature over falloff** (warm core → dim desaturated rim) + **specular N·H glints** (both a few ALU in `accumulateLights`), and the flagship — the user's **decay lightmap**: a third, COARSE (512×256 RGBA16F, [F1](2026-07-27-lighting-feel/forks.md#f1)), *ephemeral* map that fades toward zero every frame via one in-place `blendFunc(ZERO, CONSTANT_COLOR)` draw ([F2](2026-07-27-lighting-feel/forks.md#f2)); light PARTICLES are splatted in additively, fire-and-forget, **shadow-STAMPED at emit** by the parent light's coarse-shadow slot ([F4](2026-07-27-lighting-feel/forks.md#f4)) so flicker can't bleed into shadow. Flicker then reads as motion while the gather submits ZERO draws — the base light sits static in the hot accumulator and re-bakes only on real movement. Exempt from the exactness machinery BY DESIGN (it forgets; nothing is ever subtracted). Plus art-gated **AO** (surface-G, audit first — believed thin) and **emissive** (leaves don't exist yet; placement [F3](2026-07-27-lighting-feel/forks.md#f3) unresolved). Documented-later intent staged in [docs/intent/lighting-feel](../intent/lighting-feel/README.md): halo, light shafts, tone grading, ground-relief-rides-tile-prims, and the parked SDF-silhouette penumbra (so iteration #6 never starts from scratch). Zero-light gameplay rule inviolate throughout. |
| [2026-07-27-lighting-standing-costs](2026-07-27-lighting-standing-costs/README.md) | done | **Cut the lighting path's fixed taxes, then make its cost proportional to dirty work** (user, 2026-07-27 — "writeup the performance improvement work"). The ~0.0015 ms/pair constant (`light-budget I2`) hides three wastes the code-read found: **(1) a standing tax** — `classPass` runs for both classes unconditionally, so a fully static frame still submits 2 prev-blits (~8 MB, feeding `uShadowPrev` which has NO consumer), 2 gathers, and 2 × 8.4 M-fragment fine draws that all discard; the gather's second attachment `oCasterD` is vestigial (no reader of `textures[1]`) and doubles write bandwidth + RT memory; **(2) a discard tax** — the dirty gate is fetch-and-discard on a fullscreen draw, so at reach 4 only 1.8 M of 8.4 M fine fragments do work while the CPU already owns the exact dirty mirror; **(3) the receiver rescan** — `receiverAt`+`billboardNormal` re-derive light-INDEPENDENT geometry (6-row bucket scan + record decodes + the 3-corner `allBillboard` test) per fine texel on every light-motion re-bake, a large share of the pair constant itself. Plan: P1 delete the dead cost (skip empty classes, gate the blit, drop the attachment), P2 instanced dirty-rect draws replacing fullscreen-discard ([F1](2026-07-27-lighting-standing-costs/forks.md#f1), `uDirty` kept as belt-and-braces then retired), P3 a persistent FINE receiver map (id/cov/baseY/normal) on its OWN billboard-only dirty channel ([F2](2026-07-27-lighting-standing-costs/forks.md#f2)), P4 re-run the budget's pair sweep on the new constants. Identity (corridor↔brute 0 mismatches + bit-identity vs the pre-change build) is the contract at every phase. Sibling of `light-budget` (stability vs throughput, [F3](2026-07-27-lighting-standing-costs/forks.md#f3)); logged its reach-dependence caveat as `I3`. Out of scope: F4 delta-threshold, coarser-lod hot bakes, walk inversion, CPU presence — owned elsewhere. |
| [2026-07-27-sim-self-heal](2026-07-27-sim-self-heal/README.md) | done | **Make the sim processes survive shard outages** (user, 2026-07-27). `master`/`worker`/`orchestrator` `.expect()` every upstream connection + the 5s subscription wait at standup and never rebuild a dead connection mid-run — one briefly-down shard kills the process, and a mid-run disconnect leaves a zombie loop. The gateway already solved this (`directory.rs`: `alive` flag cleared by the SDK + rebuild-on-next-use + subscribe-and-wait). Plan: lift that pattern into a shared **`server/uplink`** crate (closure-built per module type, sub-`on_error`-aware, capped backoff — [F1–F3](2026-07-27-sim-self-heal/forks.md)), convert all three binaries so a pass skips only work whose uplink is dead (correctness rides existing idempotence: replay-safe writes, stateless grouping, durable tic), and fix the adjacent permanent-fault hazard: `server/edge`'s asset paths `.unwrap()` `RwLock`s, so one panic while holding a lock poisons content/texture serving until restart. Acceptance is live chaos: restart SpacetimeDB mid-run → all three reconnect without process restarts and the wolf resumes moving; start them with SpacetimeDB down → they come alive when it does. EXPANDED 2026-07-28 (first-pawns supplied live evidence of every failure mode in one session — the VOID-WRITING worker after a module republish ([I1](2026-07-28-first-pawns/issues.md)), the ZOMBIE npc issuing trips into a dead socket after an edge redeploy, a mere build re-triggering a data-wiping republish via `target/` in the module input-hash, and `bin/sim run` silently running a stale binary after a failed build): P4 adds the CLIENT-side heal (core engine reconnect; until then `run_brain` exits on `Disconnected` + docker restart policy), and P5 adds **dev-redeploy hardening** (user, 2026-07-28) — exclude build artifacts from module hashes, auto-bounce running sim consumers after any module/edge publish, and `bin/sim` refusing stale binaries + warning on the docker mtime miss. |
| [2026-07-26-textile-slot](2026-07-26-textile-slot/README.md) | open | **P0–P3, P5, P6 DELIVERED 2026-07-26.** Every textile map is now sized in TILES on a fixed **32×16 slot** grid (28×12 visible + 2 overscan/side, both axes pow2 so the toroidal wrap is a bitmask — [F5](2026-07-26-textile-slot/forks.md#f5)); a slot holds `2^k × 2^k` tiles at lod k, so the **texture is constant while the world it covers grows 4× per step**. `SQUARE` 64 → **128**, max art size 128px, `ZOOM_MIN` 0.25 ([B-1](2026-07-26-textile-slot/blockers.md)). Measured: **394 MiB resident, CONSTANT** across a full 1→0.25→1 sweep, against 264 MiB for the lightmap+shadow *alone* at zoom 0.25 before and still growing. **Zoom no longer flashes** — a lod change REPROJECTS instead of clearing ([P3](2026-07-26-textile-slot/todo.md)), so zoom-IN bakes **0 fresh** tiles and zoom-OUT bakes only the newly exposed ring; empty tiles drain before merely-stale ones ([P5](2026-07-26-textile-slot/todo.md)). Solved three unrelated problems at once: memory stopped tracking zoom, **gameplay stopped tracking monitor** (a fixed 3584×1536 reference + cover fit `s = max(W/3584, H/1536)` means every player sees the same world, a bigger display magnifying rather than revealing more), and the zone subscription now sizes itself from the tile window rather than a screen estimate that `SQUARE` 64→128 had silently halved ([I8](2026-07-26-textile-slot/issues.md#i8)). Two findings worth keeping: **lod does not affect sharpness** — the display ratio is `1/σ` and the `2^k` *cancels* ([I1](2026-07-26-textile-slot/issues.md#i1)) — and per-light lod state must be the **coarsest** since last cast, not the last, or a down-then-up round trip leaks light permanently ([I2](2026-07-26-textile-slot/issues.md#i2)). **NEAREST is the universal rescale rule** ([F3](2026-07-26-textile-slot/forks.md#f3)), independently mandatory for the shadow bitfield, `zdepth`'s encoded row, and the additive lightmap's exactness. Only **P4** remains (per-light `coarsest_lod`), blocked on [primitive-graph P10](2026-07-25-primitive-graph/todo.md) — whose **memory gate this stream cleared**: `RGBA32F` costs 128 MiB fixed here vs 465 MB world-sized. |
| [2026-07-27-lineart-lora](2026-07-27-lineart-lora/README.md) | open | **Train on DECOLOURED sprites so markings become geometry** (user, 2026-07-27). Targets a measured ceiling: marking *structure* was immovable through every inference lever — LoRA strength (no effect), CFG (24% metric spread, visually imperceptible), and prompt ("cross fox", "banded tail" changed **tone**, not structure) all landed in the same 0.11–0.14 band, because the corpus holds **one fox** and the LoRA learned its one light/dark layout. Colour is the wrong axis anyway — the game re-tints downstream. Transform = the user's: run the existing layer split and rebuild as `residual + Σ(layers×TINT)`, which removes **only hue** (the round-trip's whole 14.3/255 mid-tone error is perpendicular hue drift; brightness survives to ~1/255). **TINT = 200 grey** by measurement ([F2](2026-07-27-lineart-lora/forks.md#f2)) — 78% of the tonal range with a 55-level gap to the white plate; at 255 the bear lands *on* the background and vanishes, at 128 dark coats crowd their own outlines. Rejected luminance thresholding (can't separate black line work from dark fur — the wolf came out 74% black) and edge detection (traces markings as hollow contours, losing the filled stripes that are the point). **P0 done: 700/701 sprites** — and it surfaced a real tooling defect ([I1](2026-07-27-lineart-lora/issues.md#i1)): `split_layers` processes all paths in ONE process, so one crashing sprite aborts the run and everything after it **silently** produces nothing (417 of 701 lost on the first attempt; per-sprite invocation fixed it). Acceptance test for P3 is deliberately falsifiable: ask for an **anteater** line drawing — no corpus body plan fits it, so inventing one beats bank lookup and returning a boar means the ceiling is the corpus either way. Feeds the two-stage architecture (generate line art → ControlNet → detail pass). |
| [2026-07-26-sprite-eval-trust](2026-07-26-sprite-eval-trust/README.md) | done | **DELIVERED 2026-07-27 — 18/18.** `iou_ref` called both run-4 failures 6/6 where `d_aspect` scored a tie; run-5 (single-variable jitter test) REFUTED the frame hypothesis and `e07` still ships. **Make the measurement agree with the eye, then fix what run-4 broke** (user, 2026-07-26). Run-4 (fresh, 1024, dim48, 15 epochs on the P2 dataset) spent **4h39m and did not ship** — and the eval caught **neither** of its two failures: south rendered head-only portraits inside a drawn white rectangle yet **passed 10/18**, and east's pose drifted from lying to **sitting** while the metric called it a **tie** (34.8% vs 34.1%). Both found by *looking*. Diagnosis — the ruler has two structural blind spots: **`d_aspect` is unsigned**, so "too long" (a proportion wobble) and "too compact" (a broken pose convention) score identically though the sign was the whole signal ([I1](2026-07-26-sprite-eval-trust/issues.md#i1)); and **everything is bbox statistics**, which cannot see a bust *inside* the box ([I2](2026-07-26-sprite-eval-trust/issues.md#i2)). Fix = **`iou_ref`**, overlap against the real corpus sprite — explicitly NOT the already-refuted `iou_control` (which compared against the *control* and failed because a wrong control faithfully obeyed scores high); reference-IoU has ground truth, so it should catch both a sitting-vs-lying drift and a framed bust ([F2](2026-07-26-sprite-eval-trust/forks.md#f2), which also rejects more box statistics and defers a CLIP scorer as a black box where trust is the point). Plus **signed** aspect error, and **auto-emitted contact sheets** so the visual check that found the truth four times is part of the harness rather than a favour. Then **P2** jitters the pinned `fill` that taught the model to draw a margin ([I3](2026-07-26-sprite-eval-trust/issues.md#i3)) while keeping P2's measured gains, **P3** isolates the pose drift (4 candidate causes, run-4 changed all at once — [I4](2026-07-26-sprite-eval-trust/issues.md#i4)), and **P4** retrains and A/Bs against a **frozen** gate. Eval-before-retrain is re-affirmed ([F1](2026-07-26-sprite-eval-trust/forks.md#f1)) — the predecessor argued it and was proved right the hard way. Body-plan agnostic: the biped/legless/humanoid LoRAs reuse this gate and sheet harness. Predecessor's P4 flywheel + P5 coherence stay in [sprite-gen-quality](2026-07-25-sprite-gen-quality/README.md). |
| [2026-07-25-sprite-gen-quality](2026-07-25-sprite-gen-quality/README.md) | open | **Raise the quality ceiling on template-free generation** (user, 2026-07-25) — successor to [template-free-generation](2026-07-25-template-free-generation/README.md) (DONE 22/22). Triggered by the **anteater**: east is genuinely good (tapered snout, hunched back, bushy tail) but south/north collapse into a cape-like blob. **P0 fixes the RULER first** ([F1](2026-07-25-sprite-gen-quality/forks.md#f1)) because the gate has *both* error types — the blobby anteater **passed** at `d_aspect` 13.2%/4.1%, while a good horned oryx was **quarantined** at `solidity=0.349` vs a 0.35 threshold (failed by 0.001, since horns enclose empty bbox area). Retraining first would score the A/B with a ruler known to be wrong in both directions. Fix = **`iou_control`**, IoU of the output silhouette against the control it was given ([F2](2026-07-25-sprite-gen-quality/forks.md#f2); rejected more bbox statistics as the same failed class, and a learned scorer as an unexplainable black box). Then **P1** replaces the hand-written `FAMILY_REP` table — the anteater got Elephant because someone typed `pachyderm->Elephant`, while 396 bank silhouettes went unconsulted — with a shape-similarity search (`--control auto`). Then **P2**, the biggest single lever ([I1](2026-07-25-sprite-gen-quality/issues.md#i1)): `prep_train.py` upscales with **LANCZOS**, blurring edges on nearly every training image (most sources ≤256px), though this art is *vector graphics rendered as bitmaps* so an edge-preserving upscale **reconstructs** rather than hallucinates; plus subject **scale is unnormalised** (bear fills the frame, cat is tiny) so the LoRA learned scale is arbitrary. **P3** retrains + multi-seed A/B, **P4** runs the first full **flywheel** cycle (generate → screen → hand-pick → retrain), **P5** tightens cross-direction coherence (0.169 generated vs **0.028** real same-animal). Every comparison is ≥3 seeds — the predecessor's single-seed baseline reported a failure *rate* as a species verdict. Body-plan agnostic throughout: the biped/legless/arthropod/humanoid LoRAs reuse `auto` + the same gate. |
| [2026-07-25-template-free-generation](2026-07-25-template-free-generation/README.md) | done | **DELIVERED 2026-07-25 — 22/22, validated on 5 species absent from training (15/15 valid).** `--control template|corpus:<Species>|family:<f>|none`; the 396-image corpus silhouette bank replaces authored templates. **Generate species we have never drawn** (user, 2026-07-25). `bin/art generate` is *reproductive, not generative*: `load_template()` `SystemExit`s without hand-authored `textures/<kind>/template.<dir>.<part>.png`, so a new animal needs the art the generator was meant to save you. Now solvable because the `rd_quadruped` LoRA took over both of the template's old jobs — it carries the **style** (which moved the measured defaults to `dn1.0/cn0.20/cn_end0.30`, ~3.4x the seed variety), and at `dn=1.0` the template's **i2i latent is fully destroyed** ([I1](2026-07-25-template-free-generation/issues.md#i1)), leaving it a silhouette source only. The LoRA holds body plan alone (`cn=0.00` still drew a correct wolf). Measured limit: a template generalizes **within a body-plan family, not across** ([I2](2026-07-25-template-free-generation/issues.md#i2)) — leopard-on-wolf succeeded (genuinely feline features), brown-bear-on-wolf failed (face-in-torso, blobby east). Stance: control becomes a **mode** (`--control template|corpus|family|none`, explicit art still default+winning), the **corpus IS the template library** (459 on-model sprites x 132 species already in `.staging/quad-lora-train` — zero authoring, [F1](2026-07-25-template-free-generation/forks.md#f1) rejected authoring a family set as a strict subset), and **quantity + auto-screening replaces precision** (`lora_eval` scores every candidate; generate N, keep survivors) — which is also the input to the self-training flywheel. Body-plan agnostic on purpose: the biped/humanoid LoRAs reuse the same control path. |
| [2026-07-25-continuation-hooks](2026-07-25-continuation-hooks/README.md) | open | **Harden the continuation system** — the machinery that stops a turn ending while unblocked, executable work remains ([`docs-authority`](docs-authority/README.md) P5/P6 built it; this stream makes it robust). P6 fixed a hook that had been **silently dead for six days**; auditing that fix across all 34 streams found two more defects, bringing the tally to **eight classification bugs** — one root cause: *the detector infers machine state from prose written for humans, in four item dialects*, and it fails **open and silent**. Two real changes: **(1) stop inferring** — a `- [ ]`/`- [x]` checkbox becomes the only thing that counts as an item (already the majority dialect, 20/34), retiring the whole heuristic layer; **(2) make the plan executable** — corpus items are paragraphs, not actions (median 142 chars, 67 over 150), which is a *cause* of premature stopping, since a vague next step is itself a reason to check in. Plus visibility (decision log, `--doctor`, warnings) so the next silent death can't happen, a resume **brief** replacing the bare nudge, an execution-intent gate so conversational turns aren't nudged, and a `/plan-phase` decomposition skill. |
| [2026-07-25-primitive-graph](2026-07-25-primitive-graph/README.md) | open | **P0–P3b LANDED 2026-07-25** (see [`completed.md`](2026-07-25-primitive-graph/completed.md)): layouts authoritative in VARIABLES · command buffer **v3** (fixed 8-px commands, ids in the header, count scan gone) · all four records on v3 · presence at 8 slots → **16 lights + 8 billboards/tile** with `shadow-cold` repacked to **u7 coverage \| u1 on-billboard** · `prim_data` node + `billboard_data` leaf · **lights are CARRIED, never placed** · the **resolve walk** with downward-forcing inheritance (`MAX_PRIM_DEPTH = 8`, the single resolve authority). No open blockers. Remaining: real nesting + rotation reconciliation, retiring the `this.lights` array, P4 dirty generalisation, P5 authoring. **PREEMPTS the lighting/shadow streams — it rewrites the records they read.** (user, 2026-07-25) A **prim** becomes a generic **composition node**: positioned, carrying **up to 4 pieces of data** (`set_a..d` + `id_a..d`, set 0 = sentinel) which may be a **billboard**, a **light**, or **another prim**, each placed *relative to its carrier*; `hot_cold` inherits downward (topmost hot wins) and `cast_shadows` too (topmost no-cast wins). Generalises every object: torch = prim{billboard, light}; pawn = prim{head, body, hand-prim, hand-prim}; hand = prim{billboard, tool-prim}. New `billboard_data` set (def + offsets) splits from `prim_data` (position + carried ids); `light_data` carries colour/reach/radius **inline** (no light-def); `definition_data` gains `u4 type`. **Self-addressing retired** → ids ride the command as **fixed 8-px commands** (`opcode | set | 7× u16 ids` + 7 payloads = 56 writes/row; the leading opcode is a real extension point and the scatter drops its count scan), which buys back a `u16` in every tile-keyed px → **16 lights + 8 billboards per tile** (was 14/7). Presence carries **leaves** (`light_presence` / `billboard_presence`) — bucketing carriers would make per-tile billboards unbounded. **Rotation is a CPU reconciliation signal, not a render input**: the shader always uses the *definition's* rotation, and a mismatch prompts a definition swap (`parent_rotation` opts into carrier inheritance). All lanes verified to sum to 32; the RED position lane is already what `encodePosition` emits. All blockers resolved (bias-8 offsets, `parent_id` + `child` bit, rotation semantics, 8-px commands, `emitter_radius` restored, `resolved_zone` added). Subsumes `light-prims`. |
| [2026-07-23-world-geometry](2026-07-23-world-geometry/README.md) | done | Establish **ONE canonical geometric model** and conform the code to it. Ratified with the user + confirmed by simulation: billboards drawn **parallel to the view plane**, ground at **65°** to it, **fictional height `z = sin65·(px.y − base)`** measured per-prim against that prim's own base, projection parameter `s = z_light/(z_light − z_point)` (pure heights ⟹ pure y), **x separable** (`G.x = light.x + s·(prim.x − light.x)`, never influences y), and **left/right y-equality** ⟹ a billboard's shadow is **one y calculation + two projected top x's + its own bottom corners**. The shadow code today runs a *different* leaning-card fiction (`2·tan65` vs `sin65`, ~4.7× apart) with `SHADOW_LIFT` papering over the seating — so "what is our geometry" has no single answer in the codebase. Blocks shadows-on-prims. |
| [2026-07-24-map-compatibility](2026-07-24-map-compatibility/README.md) | open | **Investigation** (no feature): which maps may be cross-referenced by world coordinate, and which may not. On-prim casting broke twice by sampling the **`zdepth` composite** from the gather by world position; it drifts on zoom. Root cause: the composites are a **`textile_slot`** family (one variable-`slotPx` slot per tile in a fixed apron atlas, `slotPx` chosen by zoom — user's name), NOT the contiguous `textile_square` [`map-model.md`](2026-07-22-lighting-rebuild/map-model.md) claims. Rule that falls out: **never address a `textile_slot` map by recomputed world coord** — only via a cache-supplied UV (the display blit). Produces the map inventory + compatibility matrix, formalises `textile_slot`, and re-plans on-prim casting on a safe in-family source (the caster buckets). |
| [2026-07-24-shadows-onto-prims](2026-07-24-shadows-onto-prims/README.md) | closed | **Attempt #3** at shadows-on-billboards. The reverted attempt's math (elevation `z = sin65·Δ`, projection to `G`, the seen-face + light-side cone culls, corridor↔brute bit-identity) was correct at a fixed zoom — only the INPUT was wrong (read the `zdepth` `textile_slot` composite by world coord → zoom drift). This stream feeds that SAME math from a **zoom-safe in-family receiver mask** ([forks F1](2026-07-24-shadows-onto-prims/forks.md#f1) — lean: bake a `textile_unit` receiver-depth map aligned with `shadow-cold`). Acceptance is a **zoom sweep**, not a fixed-zoom shot. Sidesteps the deferred map-compatibility F1. Supersedes `2026-07-23-shadows-on-prims`. |
| [2026-07-23-shadows-on-prims](2026-07-23-shadows-on-prims/README.md) | closed | **SUPERSEDED by [`2026-07-24-shadows-onto-prims`](2026-07-24-shadows-onto-prims/README.md).** Attempt #2 — built + reverted (zoom regression: read the `zdepth` `textile_slot` composite by world coord). Kept for its [`completed.md`](2026-07-23-shadows-on-prims/completed.md) — the detailed, correct-at-fixed-zoom gather math that attempt #3 reuses verbatim — and the [map-compatibility](2026-07-24-map-compatibility/README.md) root cause it drove out. |
| [2026-07-23-penumbra](2026-07-23-penumbra/README.md) | delivered | **P0–P2 verified 2026-07-23.** Emitter-based **soft shadows** — make shadows soften with the light's `emitter_radius` + shadow stretch (real penumbra, not just edge AA). **P0 done**: shadow coverage u8→**u9** (512 levels; 14 high bits in the shadow texel's A reserve). Emitter multi-tap: penumbra (soft edges) + umbra (dark core receding with distance = base-dark → tip-light). u8→u9 coverage. 9-tap at display cap; 0 mismatches. |
| [2026-07-23-presence-in-data](2026-07-23-presence-in-data/README.md) | delivered | **P0–P3 verified 2026-07-23.** Fold **light-presence** + **caster-buckets** into the unified data texture as two more sets so they ride the SAME scatter pass — kills the second-FBO problem their separate textures created. Address on a **fixed region-sized torus** (1024×64 = 65 536 = one region's tiles; zone-strip fold; anchor-centred closest-wins) → deterministic, thrash-resistant (16-zone hysteresis), pan/zoom moves reads not writes; `map-model.md` at max rigidity. Self-address costs 1 of 8 slots (7 lights / 7 casters). Two-layer eviction (presence on region exit, prims/lights on zone eviction — reach gap makes it self-safe) + prim free-list. **Principle: DATA holds PERSISTENT state only** (dirty stays out). Planned; P0 = VARIABLES layouts. |
| [2026-07-23-unified-data](2026-07-23-unified-data/README.md) | delivered | **P0–P4 complete + verified 2026-07-23.** ONE 1024×1024 RGBA32UI **data texture** in 16 u16-addressable SETS (defs/prims/lights + constants; **records self-address** — u16 id in R's high half) fed by ONE 64×64 **command buffer** (v2.1 pure-payload fills: header of 16 u6 per-set counts + u8 opcode; replay-idempotent, rotating cursor) applied by point-scatter draws. CPU never uploads the data texture; gather binds 5 samplers; identity 0 mismatches, orbit at display cap. Post-mortems: ANGLE freeze + overlay stall (issues.md). |
| [2026-07-23-def-frame-anchors](2026-07-23-def-frame-anchors/README.md) | open | Rework `billboard_definition_data` onto the **whole-px-per-unit** model: pow2-square frames addressed by **u4 lod exponent** (+ u3 span, F1) instead of w/h; **minimum bbox in even units** + unsigned frame-relative offset; **u11 px nudge** (x-center, bottom-align); **3×3 frame anchors** (u2+u2) for placing the bbox on a prim's reported x/y. Kills fractional px-per-unit at the type level, frees bits, generalizes anchoring. P0 = ratify the amended layout; P5 (size off the DSL) user-deferred. |
| [2026-07-22-lighting-rebuild](2026-07-22-lighting-rebuild/README.md) | delivered | **Full teardown + clean re-implement** of the billboard-quad shadow system — **P0–P6 complete + verified 2026-07-23**: data textures (light pos/colour/reach, per-tile ≤8 lights + ≤8 casters), **geometric point-in-quad** occlusion at every zoom, **opaque-bbox** quads, **silhouette-textured** shadows (surface coverage, in-range by construction), **scoped dirty routing** for moving lights, and the **corridor re-earned bit-identical to brute force**. Open follow-ups live in its forks: [F1](2026-07-22-lighting-rebuild/forks.md#f1) LOD reconciliation, F2/C5 multi-page silhouettes. Supersedes `2026-07-22-shadow-corridor` (archived → `~/archive/shadow-corridor/`). |
| [docs-authority](docs-authority/README.md) | open | This system: front-door index + `docs-check`/`work-check` audits + the enforcement hooks. |
| [2026-07-21-showrt](2026-07-21-showrt/README.md) | open | Port the `/showRT` render-texture preview panel onto `client/webgl` — the one remaining W4f debug command ([`webgl-engine`](webgl-engine/README.md) descoped it: "needs a GL→DOM readback"). The data source (`Viewport.renderTextures()`) + `/overlayRT` already exist; missing is the panel + the GL→DOM transport. Recommended: **one grid `RenderTarget` composited in the viewport ctx + a single throttled `gl.readPixels` → a DOM `<canvas>`** (fewest stalls), on a `DomPanel` shell (native scroll drops pixijs's hand-rolled scrollbar). |
| [cold-data-textures](cold-data-textures/README.md) | done | Move cold lights + casters off per-frame instance attrs into persistent `RGBA32UI` **data textures** (light → LUT → prim indirection) read by the shader via `texelFetch` — no per-frame cold upload. The `caster-lut` C5 data layer; unblocks `shadow-projection` P5 (rotation in the LUT). Layouts in `VARIABLES.md`. |
| [webgl-engine](webgl-engine/README.md) | open | **Retire PixiJS** — a new adjacent client (`client/webgl`) on a bespoke WebGL2 renderer. We use a thin slice of Pixi (render backend + Sprite/Graphics/Text) and it's exactly the slice blocking ES 3.00 (integer textures / MRT / VTF all hit Pixi walls). Copy the non-Pixi code (DOM UI, atlas, DSL, sync, shaders), rewrite the render seams ourselves, reach parity, retire Pixi. `caster-lut` C5 becomes W7 (clean, on the owned engine). |
| [shard-tables](shard-tables/README.md) | open | The `*_tables!` generalization: fold every composing shard into generic table macros; all writes via events. P1–P3 live, P4 pending. |
| [art-metadata](art-metadata/README.md) | open | Per-variant `meta.json` sidecar from `bin/art` (channel tints + shadow outline). P1–P2 done. |
| [shadow-tiered](shadow-tiered/README.md) | done | The shadow rework fixing `shadow-world`'s aliasing: cast the realtime lights in **screen space** (window-bounded) → `screen-shadow-*`, merge into the **world-space** `shadow-*` bitfield (clear dirty bits + OR the remapped screen field, one pass), display `shadow` OR `screen-shadow`. **T1–T7 verified 2026-07-20** — no aliasing on pan, moved lights re-cast same-frame; the stale-on-pan slot-invalidation (I-9) fixed during verify. Graduates toward **shadows**. |
| [mrt-bakes](mrt-bakes/README.md) | done | Collapse the four G-buffer bake passes (`albedo`/`surface`/`normal`/`zdepth_world`) into **one MRT pass** — ~4× fewer bake draws. One ES 3.00 shader with `layout(location=0..3) out` into a `RenderTarget({colorTextures:[...]})` (MRT is Pixi-native; ES 3.00 multi-out is the enabler). Real work = merging the four bake shaders into one fragment (shared `discard`, tier branch). Behaviour-preserving. **B1–B5 verified 2026-07-20**: one merged ES 3.00 fragment (4 outs) + a 4-attachment `RenderTarget` scratch (manual `gl.drawBuffers` — Pixi omits it); all four channels pixel-identical. Dead per-channel code cleanup spawned as a follow-up. |
| [es300-migration](es300-migration/README.md) | done | Migrated **every** client shader off Pixi high-shader (ES 1.00) onto raw **ES 3.00** — one standard. **M1–M5 verified 2026-07-20.** Linchpin collapsed to a one-bit `#version 300 es` wrapper (`compileHighShaderGlProgramES300`) — Pixi's templates are version-agnostic, so its camera plumbing rode along; no hand-rolled builder ([D-1](es300-migration/deviations.md)). All 9 programs swapped + verified identical; `grep compileHighShaderGlProgram` is clean. Integer-texture / MRT wins now uniform follow-ons. |
| [es300-hello](es300-hello/README.md) | done | Prove a hand-written raw **GLSL ES 3.00** shader compiles + renders in the client (everything else is high-shader ES 1.00). **E1–E3 verified 2026-07-20**: a `/es300` full-viewport checkerboard from `uint`/bitwise (syntax ES 1.00 can't compile) renders, coexisting with the ES 1.00 scene. Finding: **Pixi v8 handles ES 3.00 natively** (just put `#version 300 es` in the fragment). Foundation for `caster-lut` C5's vertex-texture-fetch GPU cast. |
| [light-data-texture](light-data-texture/README.md) | done | Feed per-light data to the shader through a **5×5 `RGBA32F` data texture** (one column per light) instead of uniforms/hardcoded colours — the mechanism dense many-lights needs. **L1–L4 verified 2026-07-20**: shadow colour read from the texture, re-rolled once/sec → per-light colour change proves the live JS→texture→shader path (float worked; ES 1.00 ruled out true `u32`). Extends `shadow-tiered`; L5 (position from texture) deferred to **shadows**. |
| [shadow-world](shadow-world/README.md) | closed | Proved world-space `shadow-cold` storage + `/overlayRT` decode (W1–W5), BUT its direct world-cast **aliased off-window lights** (raw `mod`, no window-bounds check) — surfaced on pan. Superseded by **shadow-tiered** (screen-cast + copy). Kept for the mapping + overlay-decode lessons. |
| [shadows](shadows/README.md) | closed | **Superseded 2026-07-21** by `2026-07-21-shadow-bitfield` (per hot-shadows F3): the world-space per-bit **dirty-rect gather** replaces this stream's screen→world 4-copy — the per-bit separation makes world-space viable, so the screen-space detour is no longer the plan. Kept for the toroidal-wrap + `/overlayRT` decode lessons. |

**Archived out-of-repo** — completed/reverted streams live in **`../archive/`** (git history retains them
at their last in-repo commit). **2026-07-20:** `bitfield-rt` (done — bitfield RT round-trip + ping-pong)
and `shadow-cast` (done — shadow-casting into a bitfield + incremental updates), which the new
`shadow-world` builds on. **2026-07-19** (`../archive/work-stripped-2026-07-19/`): `texture-restructure`,
`spacetime-again`, `coord-purge`, `cold-rework` (→ **shard-tables**), `docs-migration`, `prim-batching`,
and `lighting` (nuked; restart is **shadows**, target in the pixijs component `intent`/`design`).

## Archived — superseded by the lighting strip (2026-07-31)

Nineteen open/paused/blocked streams targeted the lighting + shadow system that
[`2026-07-31-lighting-strip`](2026-07-31-lighting-strip/README.md) deleted. They are **superseded, not
delivered** — the distinction matters, because a reader deciding whether an idea was tried and
rejected or simply overtaken needs to be able to tell:

`2026-07-21-shadow-bitfield` · `2026-07-23-incremental-presence` · `2026-07-23-lighting` ·
`2026-07-24-light-prims` · `2026-07-24-lightmap-resolution` · `2026-07-24-normal-tilt` ·
`2026-07-24-shadow-edge-refine` · `2026-07-24-world-space-lighting` · `2026-07-26-moving-lights` ·
`2026-07-26-torch-thing` · `2026-07-27-light-budget` · `2026-07-27-plane-intersection` ·
`2026-07-28-ns-shadows` · `2026-07-29-tile-lighting` · `2026-07-30-binary-shadow-ids` ·
`2026-07-30-shadow-polish` · `caster-lut` · `hot-shadows` · `shadow-projection`

Moved to `../archive/lighting-strip-superseded/` (out of the repo — an in-repo `archive/` gets read as
current). Their findings did not go with them: the load-bearing ones were carried into
[`2026-07-31-lighting-rework/issues.md`](2026-07-31-lighting-rework/issues.md) before the move.

**Kept open deliberately:** `2026-07-29-analytic-wall-normals` and `2026-07-29-marigold-linked-normals`
are **art pipeline**, not renderer. Their output — the normal and depth maps — survives the strip and
the rework wants it.

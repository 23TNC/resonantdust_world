# Component — `dev/textures`

_Developer/gameplay: texture + sprite creation. Docs here; the assets live in `textures/` +
templates, the scripts in `bin/art` (→ future `dev/scripts/art`). Last updated: 2026-07-14._

- **[`design/`](design/)** — [`art-style.md`](design/art-style.md) (the visual style spec),
  [`texture-paths.md`](design/texture-paths.md) + [`texture-layout/`](design/texture-layout/)
  (the folder-per-variant texture layout).

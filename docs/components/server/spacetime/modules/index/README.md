# Component — `index` (directory + presence)

_Path: `server/spacetime/server/modules/index`. **Live.** Last updated: 2026-07-15._

The routing directory. Two independent tiers sharing a database — nothing joins across them.

- **[`intent/`](intent/)** — what goes in, who reads it, why the tiers are separate.
- **[`current/`](current/)** — where it is, including the vestigial half.

Shape: [`TABLES.md` §`index`](../../../../../TABLES.md).

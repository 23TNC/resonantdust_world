square = 128          (tile size = 128px)
unit = square/16 = 8  (unit size = 8px)


offsets are signed bias -128

RGBA32UI vec<u128> object_data
  pixel 0:
    RED:    u16 unit.x, u16 unit.y
    GREEN:  u8  unit.z 
    BLUE:
    ALPHA:  u32 vec<u4> set (8 sets) 
  pixel[0..7]:
    RED:    u8 unit.x.offset, u8 unit.y.offset, u8 unit.z.offset, u4 layer, u3 roation, u1 reserved
    GREEN:  u4 set, u16 index
    BLUE:   
    ALPHA:  

frame.x (x position in units to locate the first map)
frame.y (y position in units to locate the first map)

because frame x/y are measured in 

RGBA32UI definition_data
  pixel 0:
    RED:    u10 frame.x, u10 frame.y, u4 frame.span
    GREEN:  
    BLUE:   
    ALPHA:  
  pixel 1:

  pixel 2:

  pixel 3:
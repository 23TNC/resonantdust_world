Create a 4×4 sprite sheet containing **[SUBJECT]** sprites in a RimWorld-inspired art style.

Requirements:

* Arrange exactly 16 separate sprites in a clean, evenly spaced 4×4 grid.
* Every sprite must face **due east**, directly toward the right edge of the image.
* The centerline from the back of each sprite through its head and muzzle must be horizontal and point directly right.
* Do not angle the sprites toward the viewer or toward the bottom of the image.
* Do not generate southeast-facing, northeast-facing, diagonal-facing, or front-facing sprites.
* The top and bottom sides of each sprite may remain visible to preserve the elevated RimWorld-style perspective, but the directional heading must remain exactly east.
* Show varied poses, silhouettes, proportions, markings, and minor design details while keeping every sprite clearly recognizable as **[SUBJECT]**.
* Use the simplified RimWorld-style visual language: compact shapes, minimal detail, limited shading, soft irregular silhouettes, and a slightly hand-painted appearance.
* Use an elevated oblique view of an east-facing subject, not a front three-quarter character portrait.
* Keep the perspective, scale, orientation, and lighting consistent across all 16 sprites.
* Center each sprite inside its grid cell with enough empty space to prevent overlap.
* Do not include text, labels, borders, grid lines, scenery, or shadows extending into neighboring cells.
* Use a solid `0xFF00FF` background across the entire image.
* Keep the background perfectly flat and uniform, with no gradients, texture, glow, or color variation.
* Do not use `0xFF00FF` anywhere inside the sprites.
* Make the sprites suitable for extraction and use in a game texture atlas.
* Output a square image at 1024×1024 resolution.

Color-region requirements:

* Design every sprite with three clearly identifiable future tint regions: **primary**, **secondary**, and **detail**.
* Render these regions as normal colored artwork, not as an RGB mask or channel-packed texture.
* The **primary** region should cover the largest main body or material area.
* The **secondary** region should cover larger markings, supporting materials, or contrasting surfaces.
* The **detail** region should cover small accents such as eyes, trim, minor markings, or small features.
* Use clearly distinguishable colors for the three regions.
* Preserve simple shading within each region, but avoid blended boundaries that would make the regions difficult to isolate.
* Do not allow shading or highlights to change a region into the identifying color of another region.
* Keep the three color regions consistent in purpose across all 16 sprites.

Subject-specific details:

**[INSERT SUBJECT DESCRIPTION, ANATOMY, MATERIALS, REGION PLACEMENT, AND COLOR GUIDANCE HERE]**

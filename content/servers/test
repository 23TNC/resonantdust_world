# content/servers/test — index routing topology for the `test` environment.
# See content/servers/dev for the full format reference. The test env is the
# harness stack: gateway :9475, server :8475 (8xxx = server, 9xxx = gateway).

# One world server, the local test game server on :8475.
server 0 ws://localhost:8475/ws

# One data shard — the test region_shard DB on the shared local daemon.
shard 0 http://start:3000 resonantdust-test-region_shard-0

# region 0x00000000 0

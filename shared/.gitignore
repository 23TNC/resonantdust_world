# Rust build artifacts — any `target/` at any depth (the workspace root plus
# per-member crate dirs, which docker builds as root).
target/
**/*.rs.bk

# Cargo lockfile is regenerated in the build image.
Cargo.lock

# wasm-bindgen output the pixijs client imports (regenerated by `compose wasm`).
/pkg/

# OS / IDE junk
.DS_Store
Thumbs.db
.idea/
*.swp

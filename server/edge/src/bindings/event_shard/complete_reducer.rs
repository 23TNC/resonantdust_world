// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{
	self as __sdk,
	__lib,
	__sats,
	__ws,
};


#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CompleteArgs {
    pub event_reference: u32,
    pub zones: Vec::<u16>,
}

impl From<CompleteArgs> for super::Reducer {
    fn from(args: CompleteArgs) -> Self {
        Self::Complete {
            event_reference: args.event_reference,
            zones: args.zones,
}
}
}

impl __sdk::InModule for CompleteArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `complete`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait complete {
    /// Request that the remote module invoke the reducer `complete` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`complete:complete_then`] to run a callback after the reducer completes.
    fn complete(&self, event_reference: u32,
zones: Vec::<u16>,
) -> __sdk::Result<()> {
        self.complete_then(event_reference, zones,  |_, _| {})
    }

    /// Request that the remote module invoke the reducer `complete` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn complete_then(
        &self,
        event_reference: u32,
zones: Vec::<u16>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl complete for super::RemoteReducers {
    fn complete_then(
        &self,
        event_reference: u32,
zones: Vec::<u16>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(CompleteArgs { event_reference, zones,  }, callback)
    }
}


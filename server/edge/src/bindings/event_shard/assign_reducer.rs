// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{
	self as __sdk,
	__lib,
	__sats,
	__ws,
};


#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AssignArgs {
    pub events: Vec::<u32>,
    pub worker: u8,
}

impl From<AssignArgs> for super::Reducer {
    fn from(args: AssignArgs) -> Self {
        Self::Assign {
            events: args.events,
            worker: args.worker,
}
}
}

impl __sdk::InModule for AssignArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `assign`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait assign {
    /// Request that the remote module invoke the reducer `assign` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`assign:assign_then`] to run a callback after the reducer completes.
    fn assign(&self, events: Vec::<u32>,
worker: u8,
) -> __sdk::Result<()> {
        self.assign_then(events, worker,  |_, _| {})
    }

    /// Request that the remote module invoke the reducer `assign` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn assign_then(
        &self,
        events: Vec::<u32>,
worker: u8,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl assign for super::RemoteReducers {
    fn assign_then(
        &self,
        events: Vec::<u32>,
worker: u8,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(AssignArgs { events, worker,  }, callback)
    }
}


// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{
	self as __sdk,
	__lib,
	__sats,
	__ws,
};


#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ResolveArgs {
    pub server_id: u16,
    pub entity_key: u64,
    pub tic: u32,
    pub zone_id: u32,
    pub things: Vec::<u64>,
}

impl From<ResolveArgs> for super::Reducer {
    fn from(args: ResolveArgs) -> Self {
        Self::Resolve {
            server_id: args.server_id,
            entity_key: args.entity_key,
            tic: args.tic,
            zone_id: args.zone_id,
            things: args.things,
}
}
}

impl __sdk::InModule for ResolveArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `resolve`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait resolve {
    /// Request that the remote module invoke the reducer `resolve` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`resolve:resolve_then`] to run a callback after the reducer completes.
    fn resolve(&self, server_id: u16,
entity_key: u64,
tic: u32,
zone_id: u32,
things: Vec::<u64>,
) -> __sdk::Result<()> {
        self.resolve_then(server_id, entity_key, tic, zone_id, things,  |_, _| {})
    }

    /// Request that the remote module invoke the reducer `resolve` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn resolve_then(
        &self,
        server_id: u16,
entity_key: u64,
tic: u32,
zone_id: u32,
things: Vec::<u64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl resolve for super::RemoteReducers {
    fn resolve_then(
        &self,
        server_id: u16,
entity_key: u64,
tic: u32,
zone_id: u32,
things: Vec::<u64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(ResolveArgs { server_id, entity_key, tic, zone_id, things,  }, callback)
    }
}


// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{
	self as __sdk,
	__lib,
	__sats,
	__ws,
};


#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RemoveArgs {
    pub worker: u8,
    pub tic: u16,
    pub entity_reference: u32,
}

impl From<RemoveArgs> for super::Reducer {
    fn from(args: RemoveArgs) -> Self {
        Self::Remove {
            worker: args.worker,
            tic: args.tic,
            entity_reference: args.entity_reference,
}
}
}

impl __sdk::InModule for RemoveArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `remove`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait remove {
    /// Request that the remote module invoke the reducer `remove` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`remove:remove_then`] to run a callback after the reducer completes.
    fn remove(&self, worker: u8,
tic: u16,
entity_reference: u32,
) -> __sdk::Result<()> {
        self.remove_then(worker, tic, entity_reference,  |_, _| {})
    }

    /// Request that the remote module invoke the reducer `remove` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn remove_then(
        &self,
        worker: u8,
tic: u16,
entity_reference: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl remove for super::RemoteReducers {
    fn remove_then(
        &self,
        worker: u8,
tic: u16,
entity_reference: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(RemoveArgs { worker, tic, entity_reference,  }, callback)
    }
}


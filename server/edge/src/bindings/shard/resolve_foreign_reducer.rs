// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{
	self as __sdk,
	__lib,
	__sats,
	__ws,
};

use super::target_state_type::TargetState;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ResolveForeignArgs {
    pub source_shard: u16,
    pub event_reference: u64,
    pub tic: u32,
    pub results: Vec::<TargetState>,
}

impl From<ResolveForeignArgs> for super::Reducer {
    fn from(args: ResolveForeignArgs) -> Self {
        Self::ResolveForeign {
            source_shard: args.source_shard,
            event_reference: args.event_reference,
            tic: args.tic,
            results: args.results,
}
}
}

impl __sdk::InModule for ResolveForeignArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `resolve_foreign`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait resolve_foreign {
    /// Request that the remote module invoke the reducer `resolve_foreign` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`resolve_foreign:resolve_foreign_then`] to run a callback after the reducer completes.
    fn resolve_foreign(&self, source_shard: u16,
event_reference: u64,
tic: u32,
results: Vec::<TargetState>,
) -> __sdk::Result<()> {
        self.resolve_foreign_then(source_shard, event_reference, tic, results,  |_, _| {})
    }

    /// Request that the remote module invoke the reducer `resolve_foreign` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn resolve_foreign_then(
        &self,
        source_shard: u16,
event_reference: u64,
tic: u32,
results: Vec::<TargetState>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl resolve_foreign for super::RemoteReducers {
    fn resolve_foreign_then(
        &self,
        source_shard: u16,
event_reference: u64,
tic: u32,
results: Vec::<TargetState>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(ResolveForeignArgs { source_shard, event_reference, tic, results,  }, callback)
    }
}


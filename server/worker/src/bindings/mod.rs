// Generated SpacetimeDB client bindings, one submodule per upstream module.
// Regenerated by `spacetime generate` (see docs/simulation-plan.md — the tooling
// that fans this out is generalized when server_simulation/edge share it). Per-crate
// for now (Phase A micro-decision).
pub mod shard;

// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{
	self as __sdk,
	__lib,
	__sats,
	__ws,
};


#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AckReleaseArgs {
    pub now_ms: u64,
    pub transfer_id: u64,
}

impl From<AckReleaseArgs> for super::Reducer {
    fn from(args: AckReleaseArgs) -> Self {
        Self::AckRelease {
            now_ms: args.now_ms,
            transfer_id: args.transfer_id,
}
}
}

impl __sdk::InModule for AckReleaseArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `ack_release`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait ack_release {
    /// Request that the remote module invoke the reducer `ack_release` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`ack_release:ack_release_then`] to run a callback after the reducer completes.
    fn ack_release(&self, now_ms: u64,
transfer_id: u64,
) -> __sdk::Result<()> {
        self.ack_release_then(now_ms, transfer_id,  |_, _| {})
    }

    /// Request that the remote module invoke the reducer `ack_release` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn ack_release_then(
        &self,
        now_ms: u64,
transfer_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl ack_release for super::RemoteReducers {
    fn ack_release_then(
        &self,
        now_ms: u64,
transfer_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(AckReleaseArgs { now_ms, transfer_id,  }, callback)
    }
}


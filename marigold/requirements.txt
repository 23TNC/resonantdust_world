# Marigold-IID de-lighting deps (see bin/marigold build).
#
# torch: `bin/marigold build` installs torch from a CUDA-12 index first (default
# cu124) so it matches the box's CUDA 12.9 driver — the default PyPI wheel targets
# cu13x and fails ("driver too old", cuda unavailable) on this driver. Override the
# CUDA build for a different driver with:
#   bin/marigold build --torch-index https://download.pytorch.org/whl/cu128
# cu124 torch (2.6.x) supports the 2080 Ti (Turing, sm_75).
torch>=2.1
# MarigoldIntrinsicsPipeline (the IID pipeline) landed in diffusers 0.33.
diffusers>=0.33
transformers>=4.41
accelerate>=0.30
pillow>=10.0
safetensors>=0.4
# scipy: required by the Marigold DEPTH pipeline's ensembling (alignment of the
# per-latent depth predictions). normals/intrinsics don't need it.
scipy>=1.11
